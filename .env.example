# Optional local defaults for install/build. Copy to .env and edit.
# .env is ignored by Git and must never be committed with real credentials.
WIFI_SSID=MyHomeWiFi
WIFI_PASSWORD=change-me

# Optional local UTC offset used as the firmware default.
# TIMEZONE_OFFSET=3
