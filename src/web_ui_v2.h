#pragma once
#include <Arduino.h>

const char WEB_UI_HTML[] PROGMEM = R"rawliteral(
<!doctype html>
<html lang="ru">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta name="theme-color" content="#0a0f13" id="theme-color-meta">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="HydroESP">
<link rel="manifest" href="/manifest.webmanifest">
<title>HydroESP-C3</title>
<style>
:root{color-scheme:dark;--bg:#090e12;--glow:rgba(50,211,151,.10);--surface:#11191f;--surface2:#172229;--surface3:#1e2b33;--text:#f4f8f8;--muted:#91a2ab;--border:#283840;--strong:#40535d;--accent:#36d399;--accent2:#1db77c;--accentText:#042217;--accentSoft:rgba(54,211,153,.13);--blue:#70aaff;--blueSoft:rgba(112,170,255,.10);--warn:#f4c35a;--warnSoft:rgba(244,195,90,.11);--danger:#ff747a;--dangerSoft:rgba(255,116,122,.11);--shadow:0 18px 55px rgba(0,0,0,.27);--radius:18px;--sidebar:222px}
:root[data-theme="light"]{color-scheme:light;--bg:#f3f6f5;--glow:rgba(21,155,108,.09);--surface:#fff;--surface2:#f7f9f8;--surface3:#eaf0ee;--text:#14201d;--muted:#66756f;--border:#dbe4e1;--strong:#bac9c4;--accent:#159b6c;--accent2:#0e8159;--accentText:#fff;--accentSoft:rgba(21,155,108,.10);--blue:#397bd5;--blueSoft:rgba(57,123,213,.08);--warn:#a96f08;--warnSoft:rgba(196,131,14,.09);--danger:#d8464e;--dangerSoft:rgba(216,70,78,.09);--shadow:0 14px 40px rgba(29,55,46,.08)}
*{box-sizing:border-box;-webkit-tap-highlight-color:transparent}html{background:var(--bg);scroll-behavior:smooth}body{margin:0;min-height:100vh;background:radial-gradient(circle at 12% -8%,var(--glow),transparent 31rem),var(--bg);color:var(--text);font:15px/1.46 -apple-system,BlinkMacSystemFont,"Segoe UI",Inter,Roboto,Arial,sans-serif;transition:background .2s,color .2s}button,input,select{font:inherit;color:inherit}button{touch-action:manipulation}button:focus-visible,input:focus-visible,select:focus-visible,.drop-zone:focus-visible{outline:2px solid var(--accent);outline-offset:2px}.hidden{display:none!important}.app{min-height:100vh;display:grid;grid-template-columns:var(--sidebar) minmax(0,1fr)}
.sidebar{position:sticky;top:0;height:100vh;padding:22px 14px;border-right:1px solid var(--border);background:color-mix(in srgb,var(--bg) 88%,transparent);backdrop-filter:blur(20px);z-index:20}.brand{display:flex;align-items:center;gap:11px;padding:0 10px 22px;font-weight:800;letter-spacing:-.025em}.brand-mark{width:36px;height:36px;border-radius:12px;display:grid;place-items:center;background:linear-gradient(145deg,#48e2aa,#16996b);color:#052218;box-shadow:0 8px 24px rgba(54,211,153,.20)}.nav{display:grid;gap:6px}.nav-btn{border:0;background:transparent;color:var(--muted);width:100%;min-height:44px;padding:10px 12px;border-radius:12px;display:flex;align-items:center;gap:11px;text-align:left;cursor:pointer;transition:.16s}.nav-btn:hover{background:var(--surface2);color:var(--text)}.nav-btn.active{background:var(--accentSoft);color:var(--accent)}.nav-icon{width:23px;text-align:center;font-size:17px}.sidebar-foot{position:absolute;left:14px;right:14px;bottom:18px;padding:0 10px;color:var(--muted);font-size:11px}.sidebar-version{display:block;color:var(--text);margin-bottom:2px}
.main{min-width:0;padding:22px 28px 54px}.topbar{max-width:1200px;margin:0 auto 20px;display:flex;justify-content:space-between;align-items:center;gap:16px}.page-title{font-size:24px;font-weight:790;letter-spacing:-.035em}.page-subtitle{color:var(--muted);font-size:13px;margin-top:2px}.top-actions{display:flex;align-items:center;gap:9px}.icon-btn{width:38px;height:38px;border-radius:12px;border:1px solid var(--border);background:var(--surface);color:var(--muted);display:grid;place-items:center;cursor:pointer;transition:.16s}.icon-btn:hover{color:var(--text);border-color:var(--strong)}.connection{display:flex;align-items:center;gap:8px;background:var(--surface);border:1px solid var(--border);border-radius:999px;padding:8px 11px;color:var(--muted);font-size:12px;white-space:nowrap}.dot{width:8px;height:8px;border-radius:50%;background:var(--muted);box-shadow:0 0 0 4px color-mix(in srgb,var(--muted) 10%,transparent)}.connection.online .dot{background:var(--accent);box-shadow:0 0 0 4px var(--accentSoft)}.connection.offline .dot{background:var(--danger);box-shadow:0 0 0 4px var(--dangerSoft)}
.content{max-width:1200px;margin:auto}.tab{display:none}.tab.active{display:block;animation:tabIn .20s ease both}@keyframes tabIn{from{opacity:0;transform:translateY(5px)}to{opacity:1;transform:none}}.grid{display:grid;gap:16px}.grid.metrics{grid-template-columns:repeat(4,minmax(0,1fr))}.grid.two{grid-template-columns:minmax(0,1.45fr) minmax(310px,.85fr)}.grid.equal{grid-template-columns:repeat(2,minmax(0,1fr))}.card{background:linear-gradient(180deg,color-mix(in srgb,var(--surface2) 78%,var(--surface)),var(--surface));border:1px solid var(--border);border-radius:var(--radius);box-shadow:var(--shadow);padding:18px}.metric{min-height:108px;display:flex;flex-direction:column;justify-content:space-between}.metric-label{font-size:12px;color:var(--muted)}.metric-value{font-size:25px;font-weight:790;letter-spacing:-.035em}.metric-note{font-size:12px;color:var(--muted);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.section-title{font-size:18px;font-weight:750;letter-spacing:-.022em;margin:0}.section-note{font-size:13px;color:var(--muted);margin:4px 0 0}.card-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:16px}
.setup-banner{display:none;margin-bottom:16px;border:1px solid color-mix(in srgb,var(--blue) 28%,var(--border));background:linear-gradient(135deg,var(--blueSoft),var(--surface));border-radius:16px;padding:15px 16px;align-items:center;justify-content:space-between;gap:16px}.setup-banner.show{display:flex}.setup-banner b{display:block}.setup-banner p{margin:3px 0 0;color:var(--muted);font-size:12px}.mode-banner{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:14px 16px;border:1px solid var(--border);border-radius:15px;background:var(--surface);margin-bottom:16px}.mode-banner.paused{border-color:color-mix(in srgb,var(--warn) 28%,var(--border));background:linear-gradient(135deg,var(--warnSoft),var(--surface))}.mode-main{display:flex;align-items:center;gap:11px}.mode-icon{width:34px;height:34px;border-radius:11px;display:grid;place-items:center;background:var(--accentSoft);color:var(--accent);font-weight:800}.mode-banner.paused .mode-icon{background:var(--warnSoft);color:var(--warn)}.mode-main b{display:block}.mode-main small{display:block;color:var(--muted);margin-top:2px}
.pump-card{min-height:360px;display:grid;grid-template-columns:minmax(230px,.82fr) minmax(290px,1.18fr);align-items:center;gap:24px;overflow:hidden}.pump-visual{display:grid;place-items:center;min-height:260px}.pump-ring{--ring:0;position:relative;width:220px;height:220px;border-radius:50%;display:grid;place-items:center;background:conic-gradient(var(--accent) calc(var(--ring)*1%),var(--surface3) 0);transition:background .16s}.pump-ring:before{content:"";position:absolute;inset:10px;border-radius:50%;background:var(--surface);border:1px solid var(--border)}.pump-button{position:relative;z-index:2;width:174px;height:174px;border-radius:50%;border:0;background:linear-gradient(145deg,var(--surface3),var(--surface));color:var(--text);cursor:pointer;box-shadow:inset 0 0 0 1px var(--border),0 16px 36px rgba(0,0,0,.18);transition:transform .15s,box-shadow .22s,background .22s}.pump-button:hover{transform:translateY(-2px)}.pump-button:active,.pump-button.holding{transform:scale(.985)}.pump-button.on{background:linear-gradient(145deg,#39d89a,#12845c);color:#041b12;box-shadow:0 0 0 10px var(--accentSoft),0 18px 44px rgba(54,211,153,.22)}.pump-button.on:after{content:"";position:absolute;inset:-12px;border:1px solid rgba(54,211,153,.30);border-radius:50%;animation:ripple 2s ease-out infinite}@keyframes ripple{0%{transform:scale(.92);opacity:.7}100%{transform:scale(1.16);opacity:0}}.pump-icon{width:38px;height:38px;margin:auto;display:block}.pump-label{display:block;font-weight:820;margin-top:8px;font-size:14px;letter-spacing:.03em}.pump-remain{display:block;font-size:12px;opacity:.72;margin-top:4px}.control-stack{display:grid;gap:14px}.control-row{display:flex;gap:8px;flex-wrap:wrap}.duration-chip{border:1px solid var(--border);background:var(--surface2);color:var(--muted);padding:9px 11px;border-radius:10px;cursor:pointer;transition:.16s;min-width:58px}.duration-chip:hover{border-color:var(--strong);color:var(--text)}.duration-chip.active{border-color:color-mix(in srgb,var(--accent) 55%,var(--border));background:var(--accentSoft);color:var(--accent)}
.info-row{display:flex;justify-content:space-between;gap:16px;padding:11px 0;border-bottom:1px solid color-mix(in srgb,var(--border) 78%,transparent)}.info-row:last-child{border:0}.info-row span:first-child{color:var(--muted)}.info-row b{text-align:right;overflow-wrap:anywhere}.toolbar{display:flex;gap:9px;align-items:center;flex-wrap:wrap}.btn{border:1px solid var(--border);background:var(--surface2);color:var(--text);border-radius:11px;min-height:42px;padding:9px 14px;cursor:pointer;font-weight:680;transition:.15s;display:inline-flex;align-items:center;justify-content:center;gap:7px}.btn:hover{transform:translateY(-1px);border-color:var(--strong)}.btn:active{transform:none}.btn:disabled{opacity:.42;cursor:not-allowed;transform:none}.btn-primary{background:var(--accent);border-color:var(--accent);color:var(--accentText)}.btn-danger{color:var(--danger);border-color:color-mix(in srgb,var(--danger) 28%,var(--border));background:var(--dangerSoft)}.btn-warn{color:var(--warn);border-color:color-mix(in srgb,var(--warn) 28%,var(--border));background:var(--warnSoft)}.btn-ghost{background:transparent}.btn-small{min-height:36px;padding:7px 10px;font-size:13px}.btn-block{width:100%}.notice{padding:12px 14px;border-radius:12px;background:var(--surface);border:1px solid var(--border);font-size:12px;color:var(--muted)}.notice.warn{background:var(--warnSoft);border-color:color-mix(in srgb,var(--warn) 25%,var(--border));color:var(--text)}.notice.good{background:var(--accentSoft);border-color:color-mix(in srgb,var(--accent) 25%,var(--border));color:var(--text)}.badge{display:inline-flex;align-items:center;gap:6px;padding:5px 9px;border-radius:999px;background:var(--surface2);color:var(--muted);font-size:11px}.badge.good{background:var(--accentSoft);color:var(--accent)}.badge.warn{background:var(--warnSoft);color:var(--warn)}.badge.danger{background:var(--dangerSoft);color:var(--danger)}
.schedule-summary{grid-template-columns:repeat(3,1fr);margin-bottom:14px}.mini-stat{padding:13px 15px;background:var(--surface);border:1px solid var(--border);border-radius:14px}.mini-stat b{font-size:19px;display:block}.mini-stat span{color:var(--muted);font-size:11px}.dirty-line{min-height:22px;display:flex;align-items:center;gap:7px;color:var(--warn);font-size:12px;margin-bottom:8px;opacity:0;transition:.2s}.dirty-line.show{opacity:1}.timeline{padding:14px 8px 10px;margin-bottom:14px;background:var(--surface);border:1px solid var(--border);border-radius:14px}.timeline-track{position:relative;height:34px;margin:0 8px;border-radius:999px;background:linear-gradient(90deg,var(--surface3),color-mix(in srgb,var(--blue) 9%,var(--surface3)),var(--surface3));overflow:visible}.timeline-mark{position:absolute;top:50%;width:8px;height:20px;border-radius:5px;background:var(--accent);transform:translate(-50%,-50%);box-shadow:0 0 0 3px var(--accentSoft)}.timeline-axis{display:flex;justify-content:space-between;color:var(--muted);font-size:10px;margin-top:7px;padding:0 4px}.schedule-list{display:grid;gap:9px}.slot{display:grid;grid-template-columns:54px minmax(120px,1fr) minmax(105px,.72fr) 40px;gap:10px;align-items:center;background:var(--surface);border:1px solid var(--border);padding:10px;border-radius:13px;animation:slotIn .18s ease both;transition:.18s}@keyframes slotIn{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}.slot.removing{opacity:0;transform:translateX(14px)}.slot-index{color:var(--muted);font-size:11px;text-align:center}.slot-del{width:38px;height:38px;border-radius:10px;border:0;background:transparent;color:var(--muted);cursor:pointer;font-size:20px}.slot-del:hover{background:var(--dangerSoft);color:var(--danger)}.empty{padding:34px 16px;text-align:center;color:var(--muted);border:1px dashed var(--border);border-radius:14px}
.schedule-builder-card{overflow:hidden;margin-bottom:16px}.builder-layout{display:grid;grid-template-columns:minmax(300px,.82fr) minmax(0,1.18fr);gap:18px;align-items:start}.builder-controls{display:grid;gap:14px}.builder-control-group{display:grid;gap:8px}.segmented{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:4px;padding:4px;background:color-mix(in srgb,var(--bg) 58%,var(--surface));border:1px solid var(--border);border-radius:12px}.seg-btn{min-height:36px;border:0;border-radius:9px;background:transparent;color:var(--muted);cursor:pointer;font-weight:700;font-size:12px;transition:.15s}.seg-btn:hover{color:var(--text)}.seg-btn.active{background:var(--surface3);color:var(--accent);box-shadow:0 2px 10px rgba(0,0,0,.12)}.solar-pair{display:grid;grid-template-columns:1fr 1fr;gap:8px}.solar-pill{display:flex;align-items:center;gap:10px;padding:11px 12px;border:1px solid var(--border);border-radius:13px;background:var(--surface)}.solar-icon{width:30px;height:30px;border-radius:10px;display:grid;place-items:center;background:var(--warnSoft);color:var(--warn);font-size:16px}.solar-pill.sunset .solar-icon{background:var(--blueSoft);color:var(--blue)}.solar-pill span{display:block;color:var(--muted);font-size:10px}.solar-pill b{display:block;font-size:16px;line-height:1.2;margin-top:1px}.builder-inline-action{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;align-items:end}.builder-preview{border:1px solid var(--border);border-radius:16px;background:linear-gradient(180deg,color-mix(in srgb,var(--surface) 88%,var(--blueSoft)),var(--surface));padding:15px}.builder-preview-head{display:flex;justify-content:space-between;align-items:flex-start;gap:12px;margin-bottom:13px}.builder-preview-head b{display:block}.builder-preview-head span{display:block;color:var(--muted);font-size:11px;margin-top:2px}.builder-daybar{position:relative;height:86px;border-radius:14px;overflow:hidden;background:linear-gradient(90deg,#111922 0%,#101820 24%,#151c22 50%,#101820 76%,#111922 100%);border:1px solid var(--border)}:root[data-theme="light"] .builder-daybar{background:linear-gradient(90deg,#dde5ea,#edf2f1 24%,#f8f2dc 50%,#edf2f1 76%,#dde5ea)}.builder-daylight{position:absolute;top:0;bottom:0;background:linear-gradient(90deg,rgba(244,195,90,.22),rgba(112,170,255,.12) 45%,rgba(244,195,90,.18));border-left:1px solid rgba(244,195,90,.45);border-right:1px solid rgba(244,195,90,.35)}.builder-window{position:absolute;top:18px;bottom:18px;border:1px solid color-mix(in srgb,var(--accent) 68%,transparent);background:color-mix(in srgb,var(--accent) 18%,transparent);border-radius:8px}.builder-cycle-layer{position:absolute;inset:0}.builder-cycle{position:absolute;top:50%;width:7px;height:26px;border-radius:999px;background:var(--accent);transform:translate(-50%,-50%);box-shadow:0 0 0 3px color-mix(in srgb,var(--accent) 14%,transparent);z-index:4}.builder-sun-marker{position:absolute;top:0;bottom:0;width:1px;background:color-mix(in srgb,var(--warn) 70%,transparent);z-index:3}.builder-sun-marker.sunset{background:color-mix(in srgb,var(--blue) 70%,transparent)}.builder-sun-marker em{position:absolute;top:5px;left:5px;font-style:normal;font-size:9px;color:var(--warn);white-space:nowrap}.builder-sun-marker.sunset em{color:var(--blue);left:auto;right:5px}.builder-axis{display:flex;justify-content:space-between;padding:5px 3px 0;color:var(--muted);font-size:9px}.builder-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px;margin-top:12px}.builder-stat{padding:10px;border-radius:11px;background:color-mix(in srgb,var(--surface2) 86%,transparent);border:1px solid var(--border);min-width:0}.builder-stat b{display:block;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.builder-stat span{display:block;color:var(--muted);font-size:9px;margin-top:2px}.builder-times{display:flex;gap:5px;flex-wrap:wrap;max-height:72px;overflow:auto;margin-top:10px;padding-right:3px}.builder-time-chip{padding:4px 7px;border-radius:8px;background:var(--accentSoft);color:var(--accent);font:10px/1.2 ui-monospace,SFMono-Regular,Menlo,Consolas,monospace}.builder-actions{display:flex;justify-content:space-between;gap:10px;align-items:center;margin-top:12px}.builder-actions .notice{flex:1}.timeline-daylight{position:absolute;inset:0 auto 0 0;background:linear-gradient(90deg,color-mix(in srgb,var(--warn) 12%,transparent),color-mix(in srgb,var(--blue) 7%,transparent),color-mix(in srgb,var(--warn) 10%,transparent));border-left:1px solid color-mix(in srgb,var(--warn) 35%,transparent);border-right:1px solid color-mix(in srgb,var(--warn) 25%,transparent);border-radius:999px}.timeline-sunline{position:absolute;top:-6px;bottom:-6px;width:1px;background:color-mix(in srgb,var(--warn) 60%,transparent);z-index:1}.timeline-sunline.sunset{background:color-mix(in srgb,var(--blue) 60%,transparent)}.timeline-mark{z-index:2}
label{display:block;color:var(--muted);font-size:12px;margin:0 0 6px}.field{min-width:0}.input{width:100%;height:42px;border:1px solid var(--border);border-radius:10px;background:color-mix(in srgb,var(--bg) 62%,var(--surface));color:var(--text);padding:0 11px;transition:.15s}.input:hover{border-color:var(--strong)}.input:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--accentSoft);outline:0}.input.invalid{border-color:var(--danger);box-shadow:0 0 0 3px var(--dangerSoft)}.input-row{display:flex;gap:8px}.input-row .input{flex:1}.form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:13px}.form-grid.three{grid-template-columns:repeat(3,minmax(0,1fr))}.step{display:grid;grid-template-columns:34px minmax(0,1fr);gap:11px;align-items:start;padding:13px 0;border-bottom:1px solid var(--border)}.step:last-child{border:0}.step-num{width:30px;height:30px;border-radius:10px;display:grid;place-items:center;background:var(--surface3);color:var(--muted);font-size:12px;font-weight:800}.step.done .step-num{background:var(--accentSoft);color:var(--accent)}.step b{display:block}.step p{margin:3px 0 9px;color:var(--muted);font-size:12px}.sample-list{display:grid;gap:7px;margin-top:10px}.sample-row{display:grid;grid-template-columns:28px 1fr auto 32px;gap:8px;align-items:center;padding:8px 9px;border:1px solid var(--border);border-radius:11px;background:var(--surface)}.sample-row span{font-size:12px}.sample-row .sample-flow{font-weight:720}.sample-del{width:30px;height:30px;border:0;border-radius:8px;background:transparent;color:var(--muted);cursor:pointer}.sample-del:hover{background:var(--dangerSoft);color:var(--danger)}.series-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-top:10px}.series-summary>div{padding:9px;border-radius:10px;background:var(--surface);border:1px solid var(--border)}.series-summary b{display:block;font-size:15px}.series-summary span{font-size:10px;color:var(--muted)}.calc-result{background:linear-gradient(145deg,var(--accentSoft),var(--blueSoft));border:1px solid color-mix(in srgb,var(--accent) 24%,var(--border));border-radius:16px;padding:16px;margin-top:16px}.result-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.result-item{padding:10px;border-radius:11px;background:color-mix(in srgb,var(--surface) 74%,transparent)}.result-item b{font-size:18px;display:block}.result-item span{font-size:11px;color:var(--muted)}.formula{font:12px/1.55 ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;color:color-mix(in srgb,var(--text) 82%,var(--accent));background:color-mix(in srgb,var(--bg) 72%,var(--surface));border:1px solid var(--border);border-radius:12px;padding:12px;margin-top:12px;overflow:auto}.science-note{border-left:3px solid var(--blue);padding:10px 12px;background:var(--blueSoft);border-radius:0 10px 10px 0;color:color-mix(in srgb,var(--text) 88%,var(--blue));font-size:12px;margin-top:14px}
.event-list{display:grid;gap:8px}.event{display:grid;grid-template-columns:86px 10px minmax(0,1fr);gap:10px;align-items:start;padding:10px 0;border-bottom:1px solid var(--border)}.event:last-child{border:0}.event-time{font-size:11px;color:var(--muted);white-space:nowrap}.event-dot{width:8px;height:8px;margin-top:5px;border-radius:50%;background:var(--blue)}.event.pump .event-dot{background:var(--accent)}.event.warn .event-dot{background:var(--warn)}.event-title{font-weight:690;font-size:13px}.event-detail{font-size:11px;color:var(--muted);margin-top:2px}.drop-zone{border:1.5px dashed var(--strong);border-radius:16px;padding:34px 18px;text-align:center;cursor:pointer;color:var(--muted);transition:.18s;background:color-mix(in srgb,var(--bg) 35%,transparent)}.drop-zone:hover,.drop-zone.drag{border-color:var(--accent);background:var(--accentSoft);color:var(--text)}.progress{height:8px;background:color-mix(in srgb,var(--bg) 75%,var(--surface));border-radius:999px;overflow:hidden;margin-top:12px}.progress-bar{height:100%;width:0;background:linear-gradient(90deg,var(--accent),#75ecc3);transition:width .16s}.mobile-nav{display:none}.toast-stack{position:fixed;right:18px;top:18px;z-index:1000;display:grid;gap:10px;max-width:min(390px,calc(100vw - 28px));pointer-events:none}.toast{pointer-events:auto;background:var(--surface2);border:1px solid var(--border);border-radius:14px;box-shadow:0 18px 48px rgba(0,0,0,.34);padding:12px 14px;display:grid;grid-template-columns:9px 1fr auto;gap:10px;align-items:start;animation:toastIn .24s ease both}.toast.out{animation:toastOut .20s ease forwards}.toast-mark{width:9px;height:9px;border-radius:50%;background:var(--accent);margin-top:5px}.toast.error .toast-mark{background:var(--danger)}.toast.warn .toast-mark{background:var(--warn)}.toast-title{font-weight:700;font-size:13px}.toast-text{font-size:12px;color:var(--muted);margin-top:2px}.toast-close{background:transparent;border:0;color:var(--muted);cursor:pointer;padding:0 0 0 8px;font-size:18px}@keyframes toastIn{from{opacity:0;transform:translateX(18px) scale(.98)}to{opacity:1;transform:none}}@keyframes toastOut{to{opacity:0;transform:translateX(18px) scale(.98)}}.modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.56);backdrop-filter:blur(5px);z-index:900;display:grid;place-items:center;padding:20px;opacity:0;pointer-events:none;transition:.18s}.modal-backdrop.open{opacity:1;pointer-events:auto}.modal{width:min(440px,100%);background:var(--surface2);border:1px solid var(--border);border-radius:18px;box-shadow:0 30px 80px rgba(0,0,0,.45);padding:20px;transform:translateY(8px) scale(.985);transition:.18s}.modal-backdrop.open .modal{transform:none}.modal h3{margin:0 0 7px;font-size:18px}.modal p{margin:0;color:var(--muted);font-size:13px}.modal-actions{display:flex;justify-content:flex-end;gap:9px;margin-top:20px}.skeleton{position:relative;overflow:hidden;background:var(--surface2)!important;color:transparent!important;border-radius:7px}.skeleton:after{content:"";position:absolute;inset:0;transform:translateX(-100%);background:linear-gradient(90deg,transparent,rgba(255,255,255,.05),transparent);animation:shimmer 1.2s infinite}@keyframes shimmer{100%{transform:translateX(100%)}}
@media(max-width:980px){.builder-layout{grid-template-columns:1fr}.grid.metrics{grid-template-columns:repeat(2,1fr)}.grid.two,.grid.equal{grid-template-columns:1fr}.pump-card{grid-template-columns:1fr 1fr}.main{padding:20px}}
@media(max-width:760px){.builder-summary{grid-template-columns:repeat(2,1fr)}.builder-actions{display:grid}.builder-inline-action{grid-template-columns:1fr}.builder-inline-action .btn{width:100%}body{padding-bottom:78px}.app{display:block}.sidebar{display:none}.main{padding:16px 14px 28px}.topbar{margin-bottom:14px}.page-title{font-size:21px}.page-subtitle{display:none}.connection{padding:7px 9px}.grid.metrics{grid-template-columns:1fr 1fr;gap:10px}.card{padding:15px;border-radius:16px}.metric{min-height:92px}.metric-value{font-size:21px}.pump-card{grid-template-columns:1fr;gap:2px}.pump-visual{min-height:236px}.pump-ring{width:198px;height:198px}.pump-button{width:156px;height:156px}.mobile-nav{position:fixed;left:0;right:0;bottom:0;display:grid;grid-template-columns:repeat(5,1fr);padding:7px max(6px,env(safe-area-inset-right)) calc(7px + env(safe-area-inset-bottom)) max(6px,env(safe-area-inset-left));background:color-mix(in srgb,var(--bg) 92%,transparent);border-top:1px solid var(--border);backdrop-filter:blur(18px);z-index:50}.mobile-nav .nav-btn{display:grid;place-items:center;gap:1px;padding:5px 2px;font-size:9px;border-radius:9px;min-height:48px}.mobile-nav .nav-icon{font-size:17px;line-height:1}.schedule-summary{grid-template-columns:repeat(3,1fr);gap:8px}.mini-stat{padding:11px}.mini-stat b{font-size:17px}.slot{grid-template-columns:34px minmax(100px,1fr) minmax(82px,.75fr) 34px;gap:6px;padding:8px}.form-grid,.form-grid.three,.result-grid{grid-template-columns:1fr 1fr}.toast-stack{top:12px;right:14px;left:14px;max-width:none}.toolbar .btn{flex:1}.mode-banner{align-items:flex-start}.event{grid-template-columns:72px 9px minmax(0,1fr)}}
@media(max-width:430px){.solar-pair{grid-template-columns:1fr}.builder-summary{grid-template-columns:1fr 1fr}.metric-note{display:none}.schedule-summary{grid-template-columns:1fr}.slot{grid-template-columns:1fr 1fr 34px}.slot-index{display:none}.slot .field:nth-child(2){grid-column:1}.slot .field:nth-child(3){grid-column:2}.slot-del{grid-column:3}.form-grid,.form-grid.three,.result-grid,.series-summary{grid-template-columns:1fr}.mode-banner{display:grid}.mode-banner .btn{width:100%}.setup-banner{display:none!important}.mobile-nav .nav-btn{font-size:8px}.sample-row{grid-template-columns:24px 1fr auto 30px}}
@media(prefers-reduced-motion:reduce){*,*:before,*:after{animation-duration:.001ms!important;animation-iteration-count:1!important;transition-duration:.001ms!important;scroll-behavior:auto!important}}
</style>
</head>
<body>
<div class="app">
  <aside class="sidebar">
    <div class="brand"><div class="brand-mark">H</div><div>HydroESP-C3</div></div>
    <nav class="nav" aria-label="Разделы">
      <button class="nav-btn active" data-tab="status"><span class="nav-icon">◉</span>Обзор</button>
      <button class="nav-btn" data-tab="schedule"><span class="nav-icon">≡</span>Расписание</button>
      <button class="nav-btn" data-tab="hydraulics"><span class="nav-icon">∑</span>Гидравлика</button>
      <button class="nav-btn" data-tab="system"><span class="nav-icon">⚙</span>Система</button>
      <button class="nav-btn" data-tab="ota"><span class="nav-icon">⇧</span>Прошивка</button>
    </nav>
    <div class="sidebar-foot"><span class="sidebar-version" id="side-version">HydroESP-C3</span><span id="side-build">локально · без облака</span></div>
  </aside>

  <main class="main">
    <header class="topbar">
      <div><div class="page-title" id="page-title">Обзор</div><div class="page-subtitle" id="page-subtitle">Состояние установки и безопасное ручное управление</div></div>
      <div class="top-actions"><button class="icon-btn" id="theme-btn" title="Тема" aria-label="Переключить тему">◐</button><div class="connection" id="connection" aria-live="polite"><span class="dot"></span><span id="connection-text">Подключение…</span></div></div>
    </header>

    <div class="content">
      <div class="setup-banner" id="setup-banner"><div><b>Первичная настройка</b><p>Подключите домашний Wi‑Fi. После сохранения контроллер перезагрузится и станет доступен по hydro.local.</p></div><button class="btn btn-primary" id="setup-open">Настроить сеть</button></div>

      <section class="tab active" id="tab-status">
        <div class="mode-banner" id="mode-banner"><div class="mode-main"><div class="mode-icon" id="mode-icon">A</div><div><b id="mode-title">Автоматика активна</b><small id="mode-note">Плановые циклы разрешены</small></div></div><button class="btn btn-warn" id="btn-automation-main">Пауза автоматики</button></div>
        <div class="grid metrics">
          <div class="card metric"><span class="metric-label">Время устройства</span><strong class="metric-value skeleton" id="stat-time">--:--:--</strong><span class="metric-note" id="stat-date">--.--.----</span></div>
          <div class="card metric"><span class="metric-label">Следующий плановый цикл</span><strong class="metric-value skeleton" id="stat-next">--:--</strong><span class="metric-note" id="stat-sync">ожидание времени</span></div>
          <div class="card metric"><span class="metric-label">Wi‑Fi</span><strong class="metric-value skeleton" id="stat-rssi">-- dBm</strong><span class="metric-note" id="stat-ssid">—</span></div>
          <div class="card metric"><span class="metric-label">Аптайм</span><strong class="metric-value skeleton" id="stat-uptime">—</strong><span class="metric-note" id="stat-ip">IP —</span></div>
        </div>

        <div class="grid two" style="margin-top:16px">
          <div class="card pump-card">
            <div class="pump-visual"><div class="pump-ring" id="pump-ring"><button class="pump-button" id="pump-button" aria-label="Удерживайте, чтобы запустить полив"><svg class="pump-icon" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M12 2.3S5.2 10 5.2 14.6A6.8 6.8 0 0 0 12 21.4a6.8 6.8 0 0 0 6.8-6.8C18.8 10 12 2.3 12 2.3Zm0 16.5a4.2 4.2 0 0 1-4.1-3.3c1 .7 2.3 1.1 3.7 1.1 2 0 3.8-.8 5-2.1v.1a4.6 4.6 0 0 1-4.6 4.2Z"/></svg><span class="pump-label" id="pump-label">УДЕРЖИВАЙТЕ</span><span class="pump-remain" id="pump-remain">0,7 с для запуска</span></button></div></div>
            <div class="control-stack"><div><h2 class="section-title">Насос</h2><p class="section-note">Запуск — удержанием. Остановка — одним действием. Причина запуска фиксируется в журнале.</p></div><div><label>Длительность ручного цикла</label><div class="control-row"><button class="duration-chip" data-duration="30">30 с</button><button class="duration-chip active" data-duration="60">1 мин</button><button class="duration-chip" data-duration="120">2 мин</button><button class="duration-chip" data-duration="180">3 мин</button></div></div><div class="info-row"><span>Состояние</span><b id="pump-state">Выключен</b></div><div class="info-row"><span>Источник запуска</span><b id="pump-source">—</b></div><div class="info-row"><span>Следующий цикл</span><b id="pump-next">--:--</b></div></div>
          </div>

          <div class="card"><div class="card-head"><div><h2 class="section-title">Готовность</h2><p class="section-note">То, что определяет работу таймерного режима прямо сейчас</p></div><span class="badge" id="ready-badge">—</span></div><div class="info-row"><span>Время</span><b id="ready-time">—</b></div><div class="info-row"><span>Автоматика</span><b id="ready-auto">—</b></div><div class="info-row"><span>Гидравлика</span><b id="ready-hydraulics">—</b></div><div class="info-row"><span>Сеть</span><b id="ready-network">—</b></div><div class="info-row"><span>Прошивка</span><b id="info-version">—</b></div><div class="notice" id="ready-notice" style="margin-top:14px">Загрузка состояния…</div></div>
        </div>

        <div class="card" style="margin-top:16px"><div class="card-head"><div><h2 class="section-title">Последние действия</h2><p class="section-note">Журнал текущей сессии. Никаких фоновых облачных отправок.</p></div><button class="btn btn-small" id="btn-open-events">Весь журнал</button></div><div class="event-list" id="event-preview"><div class="empty">Загрузка…</div></div></div>
      </section>

      <section class="tab" id="tab-schedule">
        <div class="mode-banner" id="schedule-mode-banner"><div class="mode-main"><div class="mode-icon">A</div><div><b id="schedule-mode-title">Автоматика</b><small>Пауза не удаляет расписание и безопасна для обслуживания</small></div></div><button class="btn btn-warn" id="btn-automation-schedule">Пауза</button></div>

        <div class="card schedule-builder-card" id="schedule-builder">
          <div class="card-head"><div><h2 class="section-title">Автопостроение расписания</h2><p class="section-note">Задайте окно полива, частоту и длительность. Сначала увидите точный план на сутках — контроллер изменится только после переноса и сохранения.</p></div><span class="badge good" id="builder-source-badge">Рассвет → закат</span></div>
          <div class="builder-layout">
            <div class="builder-controls">
              <div class="solar-pair"><div class="solar-pill"><div class="solar-icon">☀</div><div><span>Рассвет сегодня</span><b id="builder-sunrise">—</b></div></div><div class="solar-pill sunset"><div class="solar-icon">◐</div><div><span>Закат сегодня</span><b id="builder-sunset">—</b></div></div></div>
              <div class="builder-control-group"><label>Границы периода</label><div class="segmented"><button class="seg-btn active" type="button" data-builder-source="solar">По рассвету и закату</button><button class="seg-btn" type="button" data-builder-source="custom">Свои часы</button></div></div>
              <div class="form-grid" id="builder-solar-controls"><div class="field"><label>Начать после рассвета, мин</label><input class="input builder-input" id="builder-start-offset" type="number" min="0" max="360" step="5" value="0"></div><div class="field"><label>Закончить до заката, мин</label><input class="input builder-input" id="builder-end-offset" type="number" min="0" max="360" step="5" value="0"></div></div>
              <div class="form-grid hidden" id="builder-custom-controls"><div class="field"><label>Начало</label><input class="input builder-input" id="builder-custom-start" type="time" value="06:00"></div><div class="field"><label>Конец</label><input class="input builder-input" id="builder-custom-end" type="time" value="20:00"></div></div>
              <div class="builder-control-group"><label>Как распределить запуски</label><div class="segmented"><button class="seg-btn active" type="button" data-builder-mode="interval">Каждые N минут</button><button class="seg-btn" type="button" data-builder-mode="count">Задать число запусков</button></div></div>
              <div id="builder-interval-controls" class="field"><label>Периодичность, мин</label><input class="input builder-input" id="builder-interval" type="number" min="5" max="720" step="5" value="60"></div>
              <div id="builder-count-controls" class="field hidden"><label>Сколько запусков за период</label><input class="input builder-input" id="builder-count" type="number" min="1" max="48" step="1" value="16"></div>
              <div class="builder-inline-action"><div class="field"><label>Длительность одного запуска, с</label><input class="input builder-input" id="builder-duration" type="number" min="1" max="3600" step="1" value="60"></div><button class="btn btn-small" type="button" id="btn-builder-hydraulics">Из гидравлики</button></div>
              <div class="section-note" id="builder-window-note">Рассвет и закат рассчитываются контроллером по координатам из раздела «Система».</div>
            </div>

            <div class="builder-preview">
              <div class="builder-preview-head"><div><b>Предпросмотр суток</b><span id="builder-window-label">Ожидание времени контроллера…</span></div><span class="badge" id="builder-mode-badge">—</span></div>
              <div class="builder-daybar" id="builder-daybar"><div class="builder-daylight" id="builder-daylight"></div><div class="builder-window" id="builder-window"></div><div class="builder-sun-marker" id="builder-sunrise-marker"><em>рассвет</em></div><div class="builder-sun-marker sunset" id="builder-sunset-marker"><em>закат</em></div><div class="builder-cycle-layer" id="builder-cycle-layer"></div></div>
              <div class="builder-axis"><span>00</span><span>06</span><span>12</span><span>18</span><span>24</span></div>
              <div class="builder-summary"><div class="builder-stat"><b id="builder-stat-count">—</b><span>запусков</span></div><div class="builder-stat"><b id="builder-stat-interval">—</b><span>периодичность</span></div><div class="builder-stat"><b id="builder-stat-duration">—</b><span>один запуск</span></div><div class="builder-stat"><b id="builder-stat-runtime">—</b><span>насос за сутки</span></div></div>
              <div class="builder-times" id="builder-times"></div>
              <div class="builder-actions"><div><div class="notice" id="builder-notice">Настройте параметры — здесь появится проверенный черновик.</div><div class="section-note" style="margin-top:6px">После сохранения получатся фиксированные часы на сутки. Рассвет и закат используются для построения; ежедневное автоматическое смещение расписания не включается скрытно.</div></div><button class="btn btn-primary" type="button" id="btn-builder-apply">Перенести в расписание</button></div>
            </div>
          </div>
        </div>

        <div class="card" id="schedule-editor"><div class="card-head"><div><h2 class="section-title">Текущее расписание</h2><p class="section-note">После автопостроения можно вручную поправить отдельные времена и длительности. До 48 циклов в сутки.</p></div><div class="toolbar"><button class="btn btn-small" id="btn-add-slot">+ Цикл</button><button class="btn btn-small btn-ghost" id="btn-reset-schedule">Исходный</button><button class="btn btn-small btn-primary" id="btn-save-schedule" disabled>Сохранить</button></div></div><div class="dirty-line" id="schedule-dirty"><span>●</span><span>Есть несохранённые изменения — контроллер пока работает по старому расписанию</span></div><div class="grid schedule-summary"><div class="mini-stat"><b id="sum-count">0</b><span>циклов / сутки</span></div><div class="mini-stat"><b id="sum-runtime">0 мин</b><span>работа насоса</span></div><div class="mini-stat"><b id="sum-duty">0%</b><span>доля суток</span></div></div><div class="timeline"><div class="timeline-track" id="timeline-track"></div><div class="timeline-axis"><span>00</span><span>06</span><span>12</span><span>18</span><span>24</span></div></div><div class="schedule-list" id="schedule-list"><div class="empty">Загрузка расписания…</div></div></div>
      </section>

      <section class="tab" id="tab-hydraulics">
        <div class="grid two">
          <div class="card"><div class="card-head"><div><h2 class="section-title">Калибровка насоса</h2><p class="section-note">Измеряем фактическую подачу установки серией повторов, а не доверяем паспортному расходу.</p></div><span class="badge" id="cal-badge">не калиброван</span></div>
            <div class="step" id="cal-step-1"><div class="step-num">1</div><div><b>Приостановите автоматику</b><p>Калибровочный тест разрешён только когда плановые циклы выключены.</p><button class="btn btn-small btn-warn" id="btn-cal-pause">Поставить автоматику на паузу</button></div></div>
            <div class="step" id="cal-step-2"><div class="step-num">2</div><div><b>Подготовьте мерную ёмкость</b><p>Направьте фактический выход установки в ёмкость. Для воспроизводимости используйте один и тот же маршрут воды и повторите тест минимум дважды, лучше 3 раза.</p><div class="control-row"><button class="duration-chip cal-duration" data-cal-duration="15">15 с</button><button class="duration-chip cal-duration active" data-cal-duration="30">30 с</button><button class="duration-chip cal-duration" data-cal-duration="60">60 с</button><button class="btn btn-primary" id="btn-cal-start">Запустить тест</button></div><div class="progress hidden" id="cal-progress"><div class="progress-bar" id="cal-progress-bar"></div></div><div class="section-note" id="cal-running" style="margin-top:7px">Тест не запущен</div></div></div>
            <div class="step" id="cal-step-3"><div class="step-num">3</div><div><b>Добавьте результаты серии</b><p>После каждого теста измерьте собранный объём. Каждый замер превращается в отдельную оценку Q; среднее и CV показывают повторяемость серии.</p><div class="input-row"><input class="input" id="cal-volume" type="number" min="1" max="100000" step="1" placeholder="Например, 850 мл"><button class="btn" id="btn-cal-add">Добавить замер</button></div><div class="sample-list" id="cal-sample-list"></div><div class="series-summary"><div><b id="cal-series-count">0</b><span>замеров</span></div><div><b id="cal-series-mean">—</b><span>средний Q</span></div><div><b id="cal-series-cv">—</b><span>CV повторяемости</span></div></div><div class="notice" id="cal-series-quality" style="margin-top:9px">Нужно минимум 2 замера; рекомендуется 3 и более.</div><div class="toolbar" style="margin-top:9px"><button class="btn btn-primary" id="btn-cal-save" disabled>Сохранить среднее</button><button class="btn btn-ghost" id="btn-cal-clear">Очистить серию</button></div><div class="notice" id="cal-result" style="margin-top:9px">Сохранённая калибровка: —</div></div></div>
            <div class="field" style="margin-top:14px"><label>Эффективная доставка воды, %</label><div class="input-row"><input class="input" id="cal-efficiency" type="number" min="10" max="100" step="1" value="85"><button class="btn" id="btn-save-efficiency">Сохранить</button></div></div>
          </div>

          <div class="card"><div class="card-head"><div><h2 class="section-title">Инженерный расчёт</h2><p class="section-note">Расход подставляется из сохранённой серии калибровки автоматически. VPD остаётся отдельным диагностическим показателем.</p></div></div><div class="form-grid three"><div class="field"><label>Фактический расход, л/мин</label><input class="input calc" id="calc-flow" type="number" min="0.05" step="0.001" placeholder="Сначала калибровка"></div><div class="field"><label>Эффективная доставка, %</label><input class="input calc" id="calc-eff" type="number" min="10" max="100" step="1" value="85"></div><div class="field"><label>Количество растений</label><input class="input calc" id="calc-plants" type="number" min="1" max="500" step="1" value="20"></div><div class="field"><label>Подача на растение / цикл, мл</label><input class="input calc" id="calc-dose" type="number" min="1" step="1" value="50"></div><div class="field"><label>Начало периода</label><input class="input calc" id="calc-start" type="time" value="06:00"></div><div class="field"><label>Конец периода</label><input class="input calc" id="calc-end" type="time" value="20:00"></div><div class="field"><label>Интервал, мин</label><input class="input calc" id="calc-interval" type="number" min="5" max="720" step="5" value="30"></div><div class="field"><label>Температура воздуха, °C</label><input class="input calc" id="calc-temp" type="number" min="5" max="45" step="0.1" value="24"></div><div class="field"><label>Относительная влажность, %</label><input class="input calc" id="calc-rh" type="number" min="10" max="100" step="1" value="65"></div></div><div class="calc-result"><div class="result-grid"><div class="result-item"><b id="res-duration">—</b><span>длительность цикла</span></div><div class="result-item"><b id="res-cycles">—</b><span>циклов за период</span></div><div class="result-item"><b id="res-volume">—</b><span>подача / сутки</span></div></div><div class="formula" id="calc-formula">Сначала выполните калибровку расхода.</div><div class="toolbar" style="justify-content:space-between;margin-top:12px"><span class="badge" id="vpd-badge">VPD —</span><button class="btn btn-primary" id="btn-generate-schedule">В конструктор расписания</button></div></div><div class="science-note">CV показывает только повторяемость серии гидравлических измерений. Даже низкий CV не доказывает правильность агрономической нормы. VPD также не является самостоятельной командой на полив.</div></div>
        </div>
      </section>

      <section class="tab" id="tab-system">
        <div class="grid equal">
          <div class="card"><div class="card-head"><div><h2 class="section-title">Сеть и время</h2><p class="section-note">Пароль никогда не выгружается обратно из контроллера.</p></div></div><div class="form-grid"><div class="field"><label>Wi‑Fi SSID</label><input class="input" id="cfg-ssid" maxlength="32" autocomplete="off"></div><div class="field"><label>Новый пароль Wi‑Fi</label><div class="input-row"><input class="input" id="cfg-pass" type="password" maxlength="63" autocomplete="new-password" placeholder="Оставьте пустым, чтобы не менять"><button class="icon-btn" id="pass-toggle" type="button" aria-label="Показать пароль">◉</button></div></div><div class="field"><label>Часовой пояс UTC</label><div class="input-row"><input class="input" id="cfg-tz" type="number" min="-12" max="14" step="1"><button class="btn btn-small" id="tz-browser" type="button">Из браузера</button></div></div><div class="field"><label>Широта</label><input class="input" id="cfg-lat" type="number" min="-90" max="90" step="0.0001"></div><div class="field"><label>Долгота</label><input class="input" id="cfg-lon" type="number" min="-180" max="180" step="0.0001"></div></div><div class="toolbar" style="margin-top:16px"><button class="btn btn-primary" id="btn-save-config">Сохранить и перезагрузить</button></div></div>
          <div class="card"><div class="card-head"><div><h2 class="section-title">Диагностика</h2><p class="section-note">Состояние контроллера без подключения Serial.</p></div><button class="btn btn-small" id="btn-refresh-diag">Обновить</button></div><div class="info-row"><span>Прошивка</span><b id="diag-version">—</b></div><div class="info-row"><span>Свободно RAM</span><b id="diag-heap">—</b></div><div class="info-row"><span>Минимум RAM</span><b id="diag-minheap">—</b></div><div class="info-row"><span>Размер flash</span><b id="diag-flash">—</b></div><div class="info-row"><span>Размер firmware</span><b id="diag-sketch">—</b></div><div class="info-row"><span>Причина рестарта</span><b id="diag-reset">—</b></div></div>
          <div class="card" style="grid-column:1/-1"><div class="card-head"><div><h2 class="section-title">Журнал текущей сессии</h2><p class="section-note">До 32 последних управляющих событий в RAM. После перезапуска журнал начинается заново.</p></div><div class="toolbar"><button class="btn btn-small" id="btn-refresh-events">Обновить</button><button class="btn btn-small btn-ghost" id="btn-clear-events">Очистить</button></div></div><div class="event-list" id="event-list"><div class="empty">Загрузка…</div></div></div>
          <div class="card"><h2 class="section-title">Резервная копия</h2><p class="section-note">Экспортирует расписание, режим автоматики и гидравлическую калибровку. Пароль Wi‑Fi не экспортируется.</p><div class="toolbar" style="margin-top:16px"><button class="btn" id="btn-export">Экспорт JSON</button><button class="btn" id="btn-import">Импорт расписания</button><input id="import-file" type="file" accept="application/json,.json" hidden></div></div>
          <div class="card"><h2 class="section-title">Сервис</h2><p class="section-note">Перезапуск не удаляет NVS-настройки.</p><div class="info-row"><span>mDNS</span><b>hydro.local</b></div><div class="info-row"><span>API</span><b id="diag-api">v—</b></div><button class="btn btn-danger btn-block" id="btn-reboot" style="margin-top:18px">Перезагрузить контроллер</button></div>
        </div>
      </section>

      <section class="tab" id="tab-ota"><div class="card" style="max-width:790px"><div class="card-head"><div><h2 class="section-title">Обновление прошивки</h2><p class="section-note">Локальный OTA из PlatformIO `.bin`. Насос перед записью останавливается автоматически.</p></div><span class="badge" id="ota-current">текущая —</span></div><div class="drop-zone" id="drop-zone" tabindex="0" role="button" aria-label="Выбрать файл прошивки"><div style="font-size:32px;margin-bottom:8px">⇧</div><b style="color:var(--text)">Перетащите .bin сюда</b><div style="margin-top:4px">или нажмите, чтобы выбрать файл</div><input id="ota-file" type="file" accept=".bin,application/octet-stream" hidden></div><div id="ota-progress" class="hidden" style="margin-top:16px"><div style="display:flex;justify-content:space-between;gap:12px"><span id="ota-text">Подготовка…</span><b id="ota-percent">0%</b></div><div class="progress"><div class="progress-bar" id="ota-bar"></div></div></div><div class="notice warn" style="margin-top:16px">OTA рассчитан на доверенную локальную сеть. Web-auth, защищённый commissioning и device-side signed OTA остаются отдельным security-этапом.</div></div></section>
    </div>
  </main>
</div>

<nav class="mobile-nav" aria-label="Разделы"><button class="nav-btn active" data-tab="status"><span class="nav-icon">◉</span>Обзор</button><button class="nav-btn" data-tab="schedule"><span class="nav-icon">≡</span>График</button><button class="nav-btn" data-tab="hydraulics"><span class="nav-icon">∑</span>Расход</button><button class="nav-btn" data-tab="system"><span class="nav-icon">⚙</span>Система</button><button class="nav-btn" data-tab="ota"><span class="nav-icon">⇧</span>Прошивка</button></nav>
<div class="toast-stack" id="toast-stack" aria-live="polite"></div>
<div class="modal-backdrop" id="modal-backdrop" aria-hidden="true"><div class="modal" role="dialog" aria-modal="true" aria-labelledby="modal-title"><h3 id="modal-title">Подтверждение</h3><p id="modal-text"></p><div class="modal-actions"><button class="btn" id="modal-cancel">Отмена</button><button class="btn btn-primary" id="modal-ok">Продолжить</button></div></div></div>

<script>
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const MAX_SLOTS=48,HOLD_MS=700,MAX_CAL_SAMPLES=9;
let activeTab='status',manualDuration=60,lastStatus=null,scheduleLoaded=false,scheduleDirty=false,modalResolve=null,holdRAF=0,holdStart=0,calDuration=30,calTestDuration=30,hydraulicsLoaded=false,calSamples=[],builderPreview=null,lastSolarSignature='';
const tabMeta={status:['Обзор','Состояние установки и безопасное ручное управление'],schedule:['Расписание','Циклы полива и суточная нагрузка насоса'],hydraulics:['Гидравлика','Повторяемая калибровка фактического расхода и инженерный расчёт'],system:['Система','Сеть, журнал, диагностика и резервное копирование'],ota:['Прошивка','Локальное OTA-обновление контроллера']};
const sourceLabel={none:'—',schedule:'по расписанию',web_manual:'веб-интерфейс',button_manual:'кнопка BOOT',calibration:'калибровка'};
const reasonLabel={none:'',manual:'остановлено вручную',timeout:'таймер завершён',reboot:'перезагрузка',ota:'обновление прошивки',automation_paused:'автоматика поставлена на паузу'};

function toast(title,text='',type='ok',timeout=3600){const el=document.createElement('div');el.className='toast '+(type==='ok'?'':type);el.innerHTML='<span class="toast-mark"></span><div><div class="toast-title"></div><div class="toast-text"></div></div><button class="toast-close" aria-label="Закрыть">×</button>';el.querySelector('.toast-title').textContent=title;el.querySelector('.toast-text').textContent=text;const close=()=>{if(el.classList.contains('out'))return;el.classList.add('out');setTimeout(()=>el.remove(),210)};el.querySelector('.toast-close').onclick=close;$('#toast-stack').appendChild(el);if(timeout)setTimeout(close,timeout)}
function ask(title,text,ok='Продолжить',danger=false){$('#modal-title').textContent=title;$('#modal-text').textContent=text;$('#modal-ok').textContent=ok;$('#modal-ok').className='btn '+(danger?'btn-danger':'btn-primary');$('#modal-backdrop').classList.add('open');$('#modal-backdrop').setAttribute('aria-hidden','false');setTimeout(()=>$('#modal-cancel').focus(),20);return new Promise(r=>modalResolve=r)}
function closeModal(value){$('#modal-backdrop').classList.remove('open');$('#modal-backdrop').setAttribute('aria-hidden','true');if(modalResolve){modalResolve(value);modalResolve=null}}
$('#modal-cancel').onclick=()=>closeModal(false);$('#modal-ok').onclick=()=>closeModal(true);$('#modal-backdrop').onclick=e=>{if(e.target.id==='modal-backdrop')closeModal(false)};document.addEventListener('keydown',e=>{if(e.key==='Escape'&&$('#modal-backdrop').classList.contains('open'))closeModal(false)});
async function api(url,opts={}){const ctl=new AbortController(),timer=setTimeout(()=>ctl.abort(),6500);try{const res=await fetch(url,{cache:'no-store',...opts,signal:ctl.signal});if(!res.ok){let msg='HTTP '+res.status;try{const j=await res.json();msg=j.error||msg}catch(_){ }throw new Error(msg)}const type=res.headers.get('content-type')||'';return type.includes('json')?await res.json():await res.text()}finally{clearTimeout(timer)}}
function fmtUptime(s){s=Math.max(0,Number(s)||0);const d=Math.floor(s/86400),h=Math.floor(s%86400/3600),m=Math.floor(s%3600/60);return d?`${d} д ${h} ч`:h?`${h} ч ${m} мин`:`${m} мин`}
function bytes(v){v=Number(v)||0;if(v>=1048576)return `${(v/1048576).toFixed(1)} МБ`;if(v>=1024)return `${Math.round(v/1024)} КБ`;return `${v} Б`}
function rssiText(v){if(!Number.isFinite(+v))return '—';const n=+v,q=n>=-55?'отлично':n>=-67?'хорошо':n>=-75?'средне':'слабо';return `${n} dBm · ${q}`}
function setConnection(online){const c=$('#connection');c.classList.toggle('online',online);c.classList.toggle('offline',!online);$('#connection-text').textContent=online?'Онлайн':'Нет связи'}
function unskeleton(){$$('.skeleton').forEach(x=>x.classList.remove('skeleton'))}
function applyTheme(mode){localStorage.setItem('hydro_theme',mode);let resolved=mode;if(mode==='auto')resolved=matchMedia('(prefers-color-scheme: light)').matches?'light':'dark';document.documentElement.dataset.theme=resolved==='light'?'light':'';$('#theme-color-meta').content=resolved==='light'?'#f3f6f5':'#0a0f13';$('#theme-btn').textContent=mode==='auto'?'◐':mode==='light'?'☀':'☾';$('#theme-btn').dataset.mode=mode}
$('#theme-btn').onclick=()=>{const now=$('#theme-btn').dataset.mode||'auto';applyTheme(now==='auto'?'light':now==='light'?'dark':'auto')};matchMedia('(prefers-color-scheme: light)').addEventListener?.('change',()=>{if(($('#theme-btn').dataset.mode||'auto')==='auto')applyTheme('auto')});applyTheme(localStorage.getItem('hydro_theme')||'auto');

function updateAutomationUi(d){const enabled=!!d.automation_enabled;for(const id of ['#mode-banner','#schedule-mode-banner'])$(id)?.classList.toggle('paused',!enabled);$('#mode-icon').textContent=enabled?'A':'Ⅱ';$('#mode-title').textContent=enabled?'Автоматика активна':'Автоматика на паузе';$('#mode-note').textContent=enabled?'Плановые циклы разрешены':'Расписание сохранено, но не запускается';$('#ready-auto').textContent=enabled?'активна':'пауза';$('#schedule-mode-title').textContent=enabled?'Автоматика активна':'Автоматика на паузе';for(const id of ['#btn-automation-main','#btn-automation-schedule']){const b=$(id);if(!b)continue;b.textContent=enabled?'Пауза автоматики':'Возобновить';b.className='btn '+(enabled?'btn-warn':'btn-primary')}$('#btn-cal-pause').disabled=!enabled;$('#btn-cal-pause').textContent=enabled?'Поставить автоматику на паузу':'Автоматика уже на паузе';$('#cal-step-1').classList.toggle('done',!enabled);$('#btn-cal-start').disabled=enabled||!!d.relay}
async function setAutomation(enabled){if(enabled){const ok=await ask('Возобновить автоматику?','Плановые циклы снова смогут запускать насос с ближайшей следующей минуты.','Возобновить');if(!ok)return}try{await api('/api/automation',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({enabled})});toast(enabled?'Автоматика включена':'Автоматика на паузе',enabled?'Расписание снова активно.':'Расписание сохранено, плановые запуски остановлены.');await updateStatus();await loadEvents()}catch(e){toast('Не удалось изменить режим',e.message,'error',5000)}}
$('#btn-automation-main').onclick=()=>setAutomation(!lastStatus?.automation_enabled);$('#btn-automation-schedule').onclick=()=>setAutomation(!lastStatus?.automation_enabled);$('#btn-cal-pause').onclick=()=>setAutomation(false);

async function updateStatus(){try{const d=await api('/api/status');lastStatus=d;syncBuilderSolarStatus(d);setConnection(true);unskeleton();$('#stat-time').textContent=d.time||'--:--:--';$('#stat-date').textContent=d.date||'—';$('#stat-next').textContent=d.next||'--:--';$('#stat-sync').textContent=d.time_synced?'время синхронизировано':'ожидание синхронизации';$('#stat-rssi').textContent=Number.isFinite(+d.rssi)?`${d.rssi} dBm`:'—';$('#stat-ssid').textContent=d.ssid||'—';$('#stat-uptime').textContent=fmtUptime(d.uptime);$('#stat-ip').textContent='IP '+(d.ip||'—');$('#pump-next').textContent=d.next||'--:--';$('#pump-source').textContent=sourceLabel[d.pump_source]||d.pump_source||'—';$('#info-version').textContent=`${d.version||'dev'} · ${d.build||'?'}`;$('#side-version').textContent=`HydroESP-C3 ${d.version||''}`;$('#side-build').textContent=`${d.build||'local'} · API v${d.api_version||'?'}`;$('#ota-current').textContent='текущая '+(d.version||'dev');$('#setup-banner').classList.toggle('show',!!d.ap_mode);$('#ready-time').textContent=d.time_synced?'синхронизировано':'нет NTP';$('#ready-network').textContent=d.ap_mode?'точка настройки':rssiText(d.rssi);$('#ready-hydraulics').textContent=d.hydraulics_calibrated?`${Number(d.pump_flow_lpm).toFixed(3)} л/мин`:'не калибрована';const ready=d.time_synced&&d.automation_enabled;const rb=$('#ready-badge');rb.textContent=ready?'готово':d.automation_enabled?'ожидание':'обслуживание';rb.className='badge '+(ready?'good':'warn');$('#ready-notice').className='notice '+(ready?'good':'warn');$('#ready-notice').textContent=ready?'Расписание может запускаться автоматически. Ручной запуск остаётся доступен удержанием.':!d.automation_enabled?'Автоматика на паузе: плановые запуски запрещены. Это безопасный режим для обслуживания и калибровки.':'Автоматика включена, но ждёт синхронизации времени.';updateAutomationUi(d);const on=!!d.relay,btn=$('#pump-button'),ring=$('#pump-ring');btn.classList.toggle('on',on);const rem=Math.max(0,+d.relay_remaining||0),prog=Math.max(0,Math.min(1,+d.relay_progress||0));if(on){$('#pump-label').textContent='ОСТАНОВИТЬ';$('#pump-remain').textContent=rem?`${rem} с осталось`:'завершение…';$('#pump-state').textContent='Насос включён';ring.style.setProperty('--ring',Math.round(prog*100));btn.setAttribute('aria-label','Остановить насос')}else if(!btn.classList.contains('holding')){$('#pump-label').textContent='УДЕРЖИВАЙТЕ';$('#pump-remain').textContent='0,7 с для запуска';$('#pump-state').textContent='Выключен';ring.style.setProperty('--ring',0);btn.setAttribute('aria-label','Удерживайте, чтобы запустить насос')}const calActive=on&&d.pump_source==='calibration';$('#cal-progress').classList.toggle('hidden',!calActive);if(calActive){$('#cal-progress-bar').style.width=Math.round(prog*100)+'%';$('#cal-running').textContent=`Калибровочный тест: осталось ${rem} с`;$('#cal-step-2').classList.add('done')}else if($('#cal-running').textContent.startsWith('Калибровочный')){$('#cal-running').textContent='Тест завершён. Измерьте объём и добавьте замер в серию.';$('#cal-progress-bar').style.width='100%'}if(!hydraulicsLoaded){applyHydraulics(d);hydraulicsLoaded=true}}catch(_){setConnection(false)}}

function switchTab(id){activeTab=id;$$('.tab').forEach(t=>t.classList.toggle('active',t.id==='tab-'+id));$$('.nav-btn[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===id));$('#page-title').textContent=tabMeta[id][0];$('#page-subtitle').textContent=tabMeta[id][1];if(id==='schedule'){if(!scheduleLoaded)loadSchedule();renderScheduleBuilder()}if(id==='system'){loadConfig();loadDiagnostics();loadEvents()}if(id==='hydraulics'){loadHydraulics();calcHydraulics();renderCalSamples()}window.scrollTo({top:0,behavior:'smooth'})}
$$('.nav-btn[data-tab]').forEach(b=>b.onclick=()=>switchTab(b.dataset.tab));$('#setup-open').onclick=()=>switchTab('system');$('#btn-open-events').onclick=()=>switchTab('system');

$$('.duration-chip:not(.cal-duration)').forEach(b=>b.onclick=()=>{$$('.duration-chip:not(.cal-duration)').forEach(x=>x.classList.remove('active'));b.classList.add('active');manualDuration=+b.dataset.duration});
async function pumpStart(){const btn=$('#pump-button');if(btn.disabled)return;btn.disabled=true;try{await api(`/api/relay/on?duration=${manualDuration}`,{method:'POST'});navigator.vibrate?.(18);toast('Насос запущен',`Ручной таймер: ${manualDuration} с.`);await updateStatus();await loadEvents()}catch(e){toast('Не удалось запустить насос',e.message,'error',5000)}finally{btn.disabled=false}}
async function pumpStop(){const btn=$('#pump-button');if(btn.disabled)return;btn.disabled=true;try{await api('/api/relay/off',{method:'POST'});navigator.vibrate?.(10);toast('Насос остановлен','Команда выполнена.');await updateStatus();await loadEvents()}catch(e){toast('Не удалось остановить насос',e.message,'error',5000)}finally{btn.disabled=false}}
function cancelHold(){if(holdRAF)cancelAnimationFrame(holdRAF);holdRAF=0;holdStart=0;const btn=$('#pump-button');btn.classList.remove('holding');if(!lastStatus?.relay){$('#pump-label').textContent='УДЕРЖИВАЙТЕ';$('#pump-remain').textContent='0,7 с для запуска';$('#pump-ring').style.setProperty('--ring',0)}}
function holdFrame(ts){if(!holdStart)holdStart=ts;const p=Math.min(1,(ts-holdStart)/HOLD_MS);$('#pump-ring').style.setProperty('--ring',Math.round(p*100));$('#pump-label').textContent='ЗАПУСК…';$('#pump-remain').textContent=`${Math.round(p*100)}%`;if(p>=1){cancelHold();pumpStart();return}holdRAF=requestAnimationFrame(holdFrame)}
function beginHold(e){if(lastStatus?.relay){pumpStop();return}if(holdRAF)return;$('#pump-button').classList.add('holding');holdStart=0;holdRAF=requestAnimationFrame(holdFrame);e?.preventDefault?.()}
const pump=$('#pump-button');pump.addEventListener('pointerdown',beginHold);['pointerup','pointercancel','pointerleave'].forEach(name=>pump.addEventListener(name,()=>{if(!lastStatus?.relay)cancelHold()}));pump.addEventListener('keydown',e=>{if((e.key==='Enter'||e.key===' ')&&!e.repeat)beginHold(e)});pump.addEventListener('keyup',e=>{if(e.key==='Enter'||e.key===' ')cancelHold()});

const BUILDER_STORAGE_KEY='hydro_schedule_builder_v1';
function clamp(v,min,max){return Math.max(min,Math.min(max,v))}
function hourFloatToMin(v){const n=Number(v);return Number.isFinite(n)?clamp(Math.round(n*60),0,1439):NaN}
function minToTime(v){if(!Number.isFinite(v))return '—';const n=((Math.round(v)%1440)+1440)%1440;return `${String(Math.floor(n/60)).padStart(2,'0')}:${String(n%60).padStart(2,'0')}`}
function shortDuration(sec){sec=Math.max(0,Math.round(Number(sec)||0));if(sec<60)return `${sec} с`;if(sec%60===0)return `${sec/60} мин`;return `${Math.floor(sec/60)} мин ${sec%60} с`}
function solarMinutes(){const sunrise=hourFloatToMin(lastStatus?.sunrise),sunset=hourFloatToMin(lastStatus?.sunset);return{sunrise,sunset,valid:!!lastStatus?.time_synced&&Number.isFinite(sunrise)&&Number.isFinite(sunset)&&sunset>sunrise}}
function positionPct(min){return clamp(min/1440*100,0,100)}
function syncBuilderSolarStatus(d){const sunrise=hourFloatToMin(d?.sunrise),sunset=hourFloatToMin(d?.sunset),sig=`${d?.time_synced?1:0}:${sunrise}:${sunset}`;$('#builder-sunrise').textContent=Number.isFinite(sunrise)?minToTime(sunrise):'—';$('#builder-sunset').textContent=Number.isFinite(sunset)?minToTime(sunset):'—';if(sig===lastSolarSignature)return;lastSolarSignature=sig;renderScheduleBuilder();if(scheduleLoaded)updateScheduleSummary()}
function setBuilderSource(source,persist=true){source=source==='custom'?'custom':'solar';$$('[data-builder-source]').forEach(b=>b.classList.toggle('active',b.dataset.builderSource===source));$('#builder-solar-controls').classList.toggle('hidden',source!=='solar');$('#builder-custom-controls').classList.toggle('hidden',source!=='custom');$('#builder-source-badge').textContent=source==='solar'?'Рассвет → закат':'Свои часы';$('#builder-source-badge').className='badge '+(source==='solar'?'good':'');if(persist)persistBuilder();renderScheduleBuilder()}
function setBuilderMode(mode,persist=true){mode=mode==='count'?'count':'interval';$$('[data-builder-mode]').forEach(b=>b.classList.toggle('active',b.dataset.builderMode===mode));$('#builder-interval-controls').classList.toggle('hidden',mode!=='interval');$('#builder-count-controls').classList.toggle('hidden',mode!=='count');if(persist)persistBuilder();renderScheduleBuilder()}
function builderSource(){return $('[data-builder-source].active')?.dataset.builderSource||'solar'}
function builderMode(){return $('[data-builder-mode].active')?.dataset.builderMode||'interval'}
function builderPrefs(){return{source:builderSource(),mode:builderMode(),startOffset:+$('#builder-start-offset').value||0,endOffset:+$('#builder-end-offset').value||0,customStart:$('#builder-custom-start').value,customEnd:$('#builder-custom-end').value,interval:+$('#builder-interval').value||60,count:+$('#builder-count').value||16,duration:+$('#builder-duration').value||60}}
function persistBuilder(){try{localStorage.setItem(BUILDER_STORAGE_KEY,JSON.stringify(builderPrefs()))}catch(_){}}
function restoreBuilder(){let p={};try{p=JSON.parse(localStorage.getItem(BUILDER_STORAGE_KEY)||'{}')||{}}catch(_){p={}};if(Number.isFinite(+p.startOffset))$('#builder-start-offset').value=clamp(+p.startOffset,0,360);if(Number.isFinite(+p.endOffset))$('#builder-end-offset').value=clamp(+p.endOffset,0,360);if(/^\d\d:\d\d$/.test(p.customStart||''))$('#builder-custom-start').value=p.customStart;if(/^\d\d:\d\d$/.test(p.customEnd||''))$('#builder-custom-end').value=p.customEnd;if(Number.isFinite(+p.interval))$('#builder-interval').value=clamp(+p.interval,5,720);if(Number.isFinite(+p.count))$('#builder-count').value=clamp(Math.round(+p.count),1,MAX_SLOTS);if(Number.isFinite(+p.duration))$('#builder-duration').value=clamp(Math.round(+p.duration),1,3600);setBuilderSource(p.source||'solar',false);setBuilderMode(p.mode||'interval',false)}
function hydraulicSuggestedDuration(){const flow=+$('#calc-flow').value,eff=+$('#calc-eff').value/100,plants=+$('#calc-plants').value,dose=+$('#calc-dose').value;if(!(flow>0&&eff>0&&eff<=1&&plants>0&&dose>0))return NaN;return 60*(plants*dose/1000)/(flow*eff)}
function builderPlan(){const source=builderSource(),mode=builderMode(),solar=solarMinutes(),duration=Math.round(+$('#builder-duration').value);if(!Number.isFinite(duration)||duration<1||duration>3600)return{error:'Длительность запуска должна быть от 1 до 3600 секунд.',source,mode,solar};let start,end;if(source==='solar'){if(!solar.valid)return{error:'Для режима «Рассвет → закат» сначала нужна синхронизация времени. Пока можно выбрать «Свои часы».',source,mode,solar};const startOffset=clamp(Math.round(+$('#builder-start-offset').value||0),0,360),endOffset=clamp(Math.round(+$('#builder-end-offset').value||0),0,360);start=solar.sunrise+startOffset;end=solar.sunset-endOffset}else{start=timeToMin($('#builder-custom-start').value);end=timeToMin($('#builder-custom-end').value)}if(!Number.isFinite(start)||!Number.isFinite(end)||start<0||end>1440||end<=start)return{error:'Конец периода должен быть позже начала в пределах одних суток.',source,mode,solar,start,end};const durationMinutes=Math.max(1,Math.ceil(duration/60)),latestStart=end-durationMinutes;if(latestStart<start)return{error:'Окно полива короче одного запуска. Увеличьте период или уменьшите длительность.',source,mode,solar,start,end};let minutes=[],interval=NaN,displayInterval='—';if(mode==='interval'){interval=Math.round(+$('#builder-interval').value);if(!Number.isFinite(interval)||interval<5||interval>720)return{error:'Периодичность должна быть от 5 до 720 минут.',source,mode,solar,start,end};for(let t=start;t<=latestStart;t+=interval){minutes.push(Math.round(t));if(minutes.length>MAX_SLOTS)break}if(minutes.length>MAX_SLOTS){const minInterval=Math.max(5,Math.ceil((latestStart-start)/(MAX_SLOTS-1)));return{error:`Получится больше ${MAX_SLOTS} запусков. Увеличьте периодичность минимум примерно до ${minInterval} мин.`,source,mode,solar,start,end}}displayInterval=`${interval} мин`}else{const count=Math.round(+$('#builder-count').value);if(!Number.isFinite(count)||count<1||count>MAX_SLOTS)return{error:`Количество запусков должно быть от 1 до ${MAX_SLOTS}.`,source,mode,solar,start,end};if(count===1){minutes=[start];displayInterval='—'}else{const span=latestStart-start;if(span<count-1)return{error:'В этом окне недостаточно минут, чтобы разместить столько отдельных запусков.',source,mode,solar,start,end};const step=span/(count-1);for(let i=0;i<count;i++)minutes.push(Math.round(start+i*step));if(new Set(minutes).size!==minutes.length)return{error:'После округления до минут некоторые запуски совпадают. Уменьшите их число.',source,mode,solar,start,end};interval=step;displayInterval=`≈ ${Math.round(step)} мин`}}if(minutes.length>1){const minGap=Math.min(...minutes.slice(1).map((m,i)=>m-minutes[i]));if(duration>minGap*60)return{error:`Запуск длится ${shortDuration(duration)}, а минимальный интервал между стартами — ${minGap} мин. Циклы будут перекрываться.`,source,mode,solar,start,end}}const slots=minutes.map(min=>({h:Math.floor(min/60),m:min%60,d:duration}));return{source,mode,solar,start,end,duration,minutes,slots,displayInterval,interval,totalRuntime:duration*slots.length,latestStart}}
function paintSolarTrack(track){const solar=solarMinutes();if(!solar.valid)return;const band=document.createElement('span');band.className='timeline-daylight';band.style.left=positionPct(solar.sunrise)+'%';band.style.width=positionPct(solar.sunset-solar.sunrise)+'%';band.title=`Световой день ${minToTime(solar.sunrise)}–${minToTime(solar.sunset)}`;track.appendChild(band);for(const [min,cls,label] of [[solar.sunrise,'','Рассвет'],[solar.sunset,' sunset','Закат']]){const line=document.createElement('span');line.className='timeline-sunline'+cls;line.style.left=positionPct(min)+'%';line.title=`${label} ${minToTime(min)}`;track.appendChild(line)}}
function renderScheduleBuilder(){if(!$('#schedule-builder'))return;const plan=builderPlan(),solar=plan.solar||solarMinutes(),daylight=$('#builder-daylight'),windowEl=$('#builder-window'),layer=$('#builder-cycle-layer'),sunriseMarker=$('#builder-sunrise-marker'),sunsetMarker=$('#builder-sunset-marker');$('#builder-sunrise').textContent=Number.isFinite(solar.sunrise)?minToTime(solar.sunrise):'—';$('#builder-sunset').textContent=Number.isFinite(solar.sunset)?minToTime(solar.sunset):'—';if(solar.valid){daylight.style.display='block';daylight.style.left=positionPct(solar.sunrise)+'%';daylight.style.width=positionPct(solar.sunset-solar.sunrise)+'%';sunriseMarker.style.display='block';sunriseMarker.style.left=positionPct(solar.sunrise)+'%';sunsetMarker.style.display='block';sunsetMarker.style.left=positionPct(solar.sunset)+'%'}else{daylight.style.display='none';sunriseMarker.style.display='none';sunsetMarker.style.display='none'}layer.innerHTML='';const notice=$('#builder-notice'),apply=$('#btn-builder-apply');if(plan.error){builderPreview=null;windowEl.style.display='none';$('#builder-window-label').textContent='Параметры требуют проверки';$('#builder-mode-badge').textContent='не готово';$('#builder-mode-badge').className='badge warn';for(const id of ['#builder-stat-count','#builder-stat-interval','#builder-stat-duration','#builder-stat-runtime'])$(id).textContent='—';$('#builder-times').innerHTML='';notice.className='notice warn';notice.textContent=plan.error;apply.disabled=true;return}builderPreview=plan;windowEl.style.display='block';windowEl.style.left=positionPct(plan.start)+'%';windowEl.style.width=positionPct(plan.end-plan.start)+'%';$('#builder-window-label').textContent=`Окно ${minToTime(plan.start)}–${minToTime(plan.end)} · первый ${minToTime(plan.minutes[0])} · последний ${minToTime(plan.minutes[plan.minutes.length-1])}`;$('#builder-mode-badge').textContent=plan.mode==='interval'?'по интервалу':'по количеству';$('#builder-mode-badge').className='badge good';$('#builder-stat-count').textContent=plan.slots.length;$('#builder-stat-interval').textContent=plan.displayInterval;$('#builder-stat-duration').textContent=shortDuration(plan.duration);$('#builder-stat-runtime').textContent=shortDuration(plan.totalRuntime);for(const min of plan.minutes){const mark=document.createElement('span');mark.className='builder-cycle';mark.style.left=positionPct(min)+'%';mark.title=`Запуск ${minToTime(min)} · ${shortDuration(plan.duration)}`;layer.appendChild(mark)}const times=$('#builder-times');times.innerHTML='';for(const min of plan.minutes){const chip=document.createElement('span');chip.className='builder-time-chip';chip.textContent=minToTime(min);chip.title=`${shortDuration(plan.duration)}`;times.appendChild(chip)}notice.className='notice '+(scheduleDirty?'warn':'good');notice.textContent=scheduleDirty?'Внизу уже есть несохранённый черновик. Перенос заменит его, но сохранение всё равно останется отдельным действием.':`Готово: ${plan.slots.length} запусков, насос ${shortDuration(plan.totalRuntime)} за сутки. Сначала переносим в черновик, затем отдельно сохраняем.`;apply.disabled=false}
async function applyBuilderSchedule(){const plan=builderPreview||builderPlan();if(!plan||plan.error){renderScheduleBuilder();return}if(scheduleDirty&&!await ask('Заменить несохранённый черновик?','Вручную изменённые ниже времена будут заменены новым автопостроением. Сохранённое в контроллере расписание пока не изменится.','Заменить'))return;const box=$('#schedule-list');box.innerHTML='';plan.slots.forEach(slot=>box.appendChild(slotRow(slot.h,slot.m,slot.d)));scheduleLoaded=true;updateScheduleSummary();markScheduleDirty(true);renderScheduleBuilder();$('#schedule-editor').scrollIntoView({behavior:'smooth',block:'start'});toast('Черновик расписания построен',`${plan.slots.length} запусков · ${minToTime(plan.start)}–${minToTime(plan.end)}. Проверьте и нажмите «Сохранить».`)}
function initScheduleBuilder(){restoreBuilder();$$('[data-builder-source]').forEach(b=>b.onclick=()=>setBuilderSource(b.dataset.builderSource));$$('[data-builder-mode]').forEach(b=>b.onclick=()=>setBuilderMode(b.dataset.builderMode));$$('.builder-input').forEach(i=>i.addEventListener('input',()=>{persistBuilder();renderScheduleBuilder()}));$('#btn-builder-hydraulics').onclick=()=>{const duration=hydraulicSuggestedDuration();if(!Number.isFinite(duration)){toast('Нет расчёта длительности','Сначала задайте расход, эффективность, число растений и дозу в разделе «Гидравлика».','warn',5000);return}$('#builder-duration').value=clamp(Math.round(duration),1,3600);persistBuilder();renderScheduleBuilder();toast('Длительность подставлена',`${shortDuration(duration)} по текущему гидравлическому расчёту.`)};$('#btn-builder-apply').onclick=applyBuilderSchedule;renderScheduleBuilder()}
function markScheduleDirty(dirty=true){scheduleDirty=dirty;$('#schedule-dirty').classList.toggle('show',dirty);$('#btn-save-schedule').disabled=!dirty;if(activeTab==='schedule')renderScheduleBuilder()}
function slotRow(h=8,m=0,d=60){const row=document.createElement('div');row.className='slot';row.innerHTML='<div class="slot-index"></div><div class="field"><label>Время</label><input class="input slot-time" type="time"></div><div class="field"><label>Длительность, с</label><input class="input slot-duration" type="number" min="1" max="3600" step="1"></div><button class="slot-del" aria-label="Удалить">×</button>';row.querySelector('.slot-time').value=`${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}`;row.querySelector('.slot-duration').value=d;row.querySelectorAll('input').forEach(i=>i.addEventListener('input',()=>{markScheduleDirty();updateScheduleSummary()}));row.querySelector('.slot-del').onclick=()=>{row.classList.add('removing');setTimeout(()=>{row.remove();renumberSlots();updateScheduleSummary();markScheduleDirty()},180)};return row}
function renumberSlots(){$$('#schedule-list .slot').forEach((r,i)=>r.querySelector('.slot-index').textContent=String(i+1).padStart(2,'0'))}
function updateScheduleSummary(){const rows=$$('#schedule-list .slot'),sec=rows.reduce((a,r)=>a+(+r.querySelector('.slot-duration').value||0),0);$('#sum-count').textContent=rows.length;$('#sum-runtime').textContent=shortDuration(sec);$('#sum-duty').textContent=(sec/864).toFixed(1)+'%';renumberSlots();const track=$('#timeline-track');track.innerHTML='';paintSolarTrack(track);rows.forEach(r=>{const min=timeToMin(r.querySelector('.slot-time').value);if(!Number.isFinite(min))return;const mark=document.createElement('span');mark.className='timeline-mark';mark.style.left=positionPct(min)+'%';mark.title=`${r.querySelector('.slot-time').value} · ${shortDuration(+r.querySelector('.slot-duration').value||0)}`;track.appendChild(mark)})}
async function loadSchedule(){try{const data=await api('/api/schedule');const box=$('#schedule-list');box.innerHTML='';data.sort((a,b)=>(a.h*60+a.m)-(b.h*60+b.m));data.forEach(slot=>box.appendChild(slotRow(slot.h,slot.m,slot.d)));if(!data.length)box.innerHTML='<div class="empty">Расписание пусто. Можно построить его автоматически выше или добавить первый цикл вручную.</div>';scheduleLoaded=true;markScheduleDirty(false);updateScheduleSummary();renderScheduleBuilder()}catch(e){$('#schedule-list').innerHTML='<div class="empty">Не удалось загрузить расписание.</div>';toast('Ошибка расписания',e.message,'error',5000)}}
function addSlot(h=8,m=0,d=60){const box=$('#schedule-list');if($$('#schedule-list .slot').length>=MAX_SLOTS){toast('Достигнут лимит',`Максимум ${MAX_SLOTS} циклов.`,'warn');return}if(!box.querySelector('.slot'))box.innerHTML='';box.appendChild(slotRow(h,m,d));updateScheduleSummary();markScheduleDirty();box.lastElementChild.scrollIntoView({behavior:'smooth',block:'nearest'})}
function collectSchedule(){const rows=$$('#schedule-list .slot');if(rows.length>MAX_SLOTS)throw new Error(`Максимум ${MAX_SLOTS} циклов`);const seen=new Set(),slots=[];let bad=false;rows.forEach(r=>{const ti=r.querySelector('.slot-time'),di=r.querySelector('.slot-duration');ti.classList.remove('invalid');di.classList.remove('invalid');const p=ti.value.split(':'),d=+di.value;if(p.length!==2){ti.classList.add('invalid');bad=true;return}const h=+p[0],m=+p[1],key=`${h}:${m}`;if(seen.has(key)){ti.classList.add('invalid');bad=true}seen.add(key);if(!Number.isFinite(d)||d<1||d>3600){di.classList.add('invalid');bad=true}slots.push({h,m,d:Math.round(d)})});if(bad)throw new Error('Проверьте время, дубли и длительность 1–3600 с');return slots.sort((a,b)=>(a.h*60+a.m)-(b.h*60+b.m))}
$('#btn-add-slot').onclick=()=>addSlot();$('#btn-save-schedule').onclick=async()=>{try{const slots=collectSchedule(),btn=$('#btn-save-schedule');btn.disabled=true;await api('/api/schedule',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(slots)});toast('Расписание сохранено',`${slots.length} циклов записано.`);await loadSchedule();await loadEvents()}catch(e){toast('Не удалось сохранить',e.message,'error',5000);$('#btn-save-schedule').disabled=false}};$('#btn-reset-schedule').onclick=async()=>{if(!await ask('Вернуть исходный график?','Текущие изменения расписания будут заменены заводским примером.','Вернуть',true))return;try{await api('/api/schedule/reset',{method:'POST'});toast('Исходный график восстановлен');await loadSchedule();await loadEvents()}catch(e){toast('Сброс не выполнен',e.message,'error',5000)}};

function calStats(){const n=calSamples.length;if(!n)return{n:0,mean:0,cv:NaN};const mean=calSamples.reduce((s,x)=>s+x.flow,0)/n;if(n<2)return{n,mean,cv:NaN};const variance=calSamples.reduce((s,x)=>s+(x.flow-mean)**2,0)/(n-1),sd=Math.sqrt(variance),cv=mean>0?sd/mean*100:NaN;return{n,mean,cv}}
function persistCalSamples(){try{localStorage.setItem('hydro_cal_samples',JSON.stringify(calSamples));localStorage.setItem('hydro_cal_duration',String(calDuration))}catch(_){}}
function renderCalSamples(){const list=$('#cal-sample-list');list.innerHTML='';calSamples.forEach((s,i)=>{const row=document.createElement('div');row.className='sample-row';row.innerHTML='<span></span><span></span><span class="sample-flow"></span><button class="sample-del" aria-label="Удалить замер">×</button>';row.children[0].textContent=String(i+1);row.children[1].textContent=`${Math.round(s.ml)} мл / ${s.duration} с`;row.querySelector('.sample-flow').textContent=`${s.flow.toFixed(3)} л/мин`;row.querySelector('.sample-del').onclick=()=>{calSamples.splice(i,1);persistCalSamples();renderCalSamples()};list.appendChild(row)});const st=calStats();$('#cal-series-count').textContent=st.n;$('#cal-series-mean').textContent=st.n?`${st.mean.toFixed(3)} л/мин`:'—';$('#cal-series-cv').textContent=Number.isFinite(st.cv)?`${st.cv.toFixed(1)}%`:'—';$('#btn-cal-save').disabled=st.n<2;const q=$('#cal-series-quality');q.className='notice';if(st.n<2){q.textContent='Нужно минимум 2 замера; рекомендуется 3 и более.'}else if(st.n<3){q.classList.add('warn');q.textContent=`Предварительная серия: 2 замера, CV ${st.cv.toFixed(1)}%. Добавьте третий замер для более устойчивой оценки.`}else if(st.cv<=5){q.classList.add('good');q.textContent=`Хорошая повторяемость серии: CV ${st.cv.toFixed(1)}%. Это характеристика разброса измерений, не агрономической точности.`}else if(st.cv<=10){q.classList.add('warn');q.textContent=`Умеренный разброс: CV ${st.cv.toFixed(1)}%. Сохранить можно, но полезен дополнительный повтор.`}else{q.classList.add('warn');q.textContent=`Высокий разброс: CV ${st.cv.toFixed(1)}%. Проверьте воздух в магистрали, ёмкость и стабильность потока перед сохранением.`}}
function applyHydraulics(d){const flow=Number(d.pump_flow_lpm??d.flow_lpm??0),eff=Number(d.delivery_efficiency_pct??d.efficiency_pct??85),samples=Number(d.calibration_sample_count??d.sample_count??0),cv=Number(d.calibration_cv_pct??d.cv_pct??0),at=d.calibrated_at||'';$('#calc-flow').value=flow>0?flow.toFixed(3):'';$('#calc-eff').value=eff;$('#cal-efficiency').value=eff;let detail=flow>0?`Сохранено: ${flow.toFixed(3)} л/мин`: 'Расход ещё не измерен.';if(flow>0&&samples)detail+=` · ${samples} зам. · CV ${cv.toFixed(1)}%`;if(flow>0&&at)detail+=` · ${at}`;$('#cal-result').textContent=detail;const b=$('#cal-badge');b.textContent=flow>0?`${flow.toFixed(3)} л/мин`:'не калиброван';b.className='badge '+(flow>0?'good':'warn');$('#cal-step-3').classList.toggle('done',flow>0);calcHydraulics()}
async function loadHydraulics(){try{const d=await api('/api/hydraulics');applyHydraulics(d);hydraulicsLoaded=true}catch(e){toast('Калибровка не загружена',e.message,'error',5000)}}
$$('.cal-duration').forEach(b=>b.onclick=()=>{$$('.cal-duration').forEach(x=>x.classList.remove('active'));b.classList.add('active');calDuration=+b.dataset.calDuration;persistCalSamples()});
$('#btn-cal-start').onclick=async()=>{if(lastStatus?.automation_enabled){toast('Сначала поставьте автоматику на паузу','Калибровочный тест не запускается при активном расписании.','warn',5000);return}if(lastStatus?.relay){toast('Насос уже работает','','warn');return}if(!await ask('Запустить калибровочный тест?',`Насос будет включён ровно на ${calDuration} с. Направьте выход в мерную ёмкость.`,'Запустить'))return;try{calTestDuration=calDuration;localStorage.setItem('hydro_last_cal_test_duration',String(calTestDuration));await api(`/api/calibration/start?duration=${calDuration}`,{method:'POST'});$('#cal-running').textContent=`Калибровочный тест ${calDuration} с запущен`;toast('Тест запущен','После остановки измерьте объём и добавьте замер.');await updateStatus();await loadEvents()}catch(e){toast('Тест не запущен',e.message,'error',5000)}};
async function saveHydraulics(flow,eff,meta=null){const payload={flow_lpm:flow,efficiency_pct:eff};if(meta){payload.sample_count=meta.sample_count;payload.cv_pct=meta.cv_pct;if(meta.calibration_epoch)payload.calibration_epoch=meta.calibration_epoch}return api('/api/hydraulics',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)})}
$('#btn-cal-add').onclick=()=>{const ml=+$('#cal-volume').value;if(!Number.isFinite(ml)||ml<=0){toast('Введите измеренный объём','Нужен фактический объём воды в миллилитрах.','warn');return}if(calSamples.length>=MAX_CAL_SAMPLES){toast('Достаточно замеров',`В серии допускается до ${MAX_CAL_SAMPLES}.`,'warn');return}const duration=+localStorage.getItem('hydro_last_cal_test_duration')||calTestDuration||calDuration,flow=ml*60/duration/1000;if(flow<.05||flow>100){toast('Получился нереалистичный расход',`${flow.toFixed(3)} л/мин выходит за допустимый диапазон 0,05–100.`,'warn',5000);return}calSamples.push({duration,ml,flow});$('#cal-volume').value='';persistCalSamples();renderCalSamples();toast('Замер добавлен',`${flow.toFixed(3)} л/мин. Повторите тест для оценки разброса.`)};
$('#btn-cal-clear').onclick=async()=>{if(!calSamples.length)return;if(!await ask('Очистить текущую серию?','Сохранённая в контроллере калибровка не изменится.','Очистить'))return;calSamples=[];persistCalSamples();renderCalSamples()};
$('#btn-cal-save').onclick=async()=>{const st=calStats(),eff=+$('#cal-efficiency').value;if(st.n<2||!Number.isFinite(st.cv)){toast('Недостаточно повторов','Нужно минимум два измерения.','warn');return}if(st.cv>10&&!await ask('Сохранить серию с высоким разбросом?',`CV ${st.cv.toFixed(1)}%. Это указывает на нестабильность измерений.`,'Сохранить'))return;try{await saveHydraulics(st.mean,eff,{sample_count:st.n,cv_pct:st.cv});toast('Калибровка сохранена',`Q = ${st.mean.toFixed(3)} л/мин · ${st.n} зам. · CV ${st.cv.toFixed(1)}%.`);await loadHydraulics();await updateStatus();await loadEvents()}catch(e){toast('Не удалось сохранить',e.message,'error',5000)}};
$('#btn-save-efficiency').onclick=async()=>{const flow=+$('#calc-flow').value||0,eff=+$('#cal-efficiency').value;try{await saveHydraulics(flow,eff);$('#calc-eff').value=eff;calcHydraulics();toast('Коэффициент сохранён',`${eff}% эффективной доставки.`);await loadEvents()}catch(e){toast('Не удалось сохранить',e.message,'error',5000)}};
function timeToMin(v){const p=(v||'').split(':');return p.length===2?(+p[0]*60+ +p[1]):NaN}
function calcHydraulics(){const flow=+$('#calc-flow').value,eff=+$('#calc-eff').value/100,plants=+$('#calc-plants').value,dose=+$('#calc-dose').value,interval=+$('#calc-interval').value,start=timeToMin($('#calc-start').value),end=timeToMin($('#calc-end').value),temp=+$('#calc-temp').value,rh=+$('#calc-rh').value;const valid=flow>0&&eff>0&&eff<=1&&plants>0&&dose>0&&interval>=5&&end>start;if(!valid){$('#res-duration').textContent='—';$('#res-cycles').textContent='—';$('#res-volume').textContent='—';$('#calc-formula').textContent=flow>0?'Проверьте исходные данные.':'Сначала выполните калибровку фактического расхода.';return null}const eventLitres=plants*dose/1000,duration=60*eventLitres/(flow*eff),cycles=Math.floor((end-start-1)/interval)+1,daily=eventLitres*cycles;$('#res-duration').textContent=duration<60?`${duration.toFixed(1)} с`:`${(duration/60).toFixed(2)} мин`;$('#res-cycles').textContent=cycles;$('#res-volume').textContent=`${daily.toFixed(1)} л`;$('#calc-formula').textContent=`Vцикла = ${plants} × ${dose} мл = ${eventLitres.toFixed(3)} л\nt = Vцикла / (${flow.toFixed(3)} л/мин × ${(eff*100).toFixed(0)}%) = ${duration.toFixed(1)} с`;if(Number.isFinite(temp)&&Number.isFinite(rh)&&rh>0&&rh<=100){const es=.6108*Math.exp(17.27*temp/(temp+237.3)),vpd=es*(1-rh/100);$('#vpd-badge').textContent=`VPD ${vpd.toFixed(2)} кПа`;$('#vpd-badge').className='badge '+(vpd<.4||vpd>1.8?'warn':'good')}else{$('#vpd-badge').textContent='VPD —';$('#vpd-badge').className='badge'}return{duration,start,end,interval}}
$$('.calc').forEach(i=>i.addEventListener('input',calcHydraulics));$('#btn-generate-schedule').onclick=()=>{const duration=hydraulicSuggestedDuration();if(Number.isFinite(duration))$('#builder-duration').value=clamp(Math.round(duration),1,3600);persistBuilder();switchTab('schedule');renderScheduleBuilder();toast('Расчёт передан в конструктор',Number.isFinite(duration)?`Длительность ${shortDuration(duration)} подставлена. Границы и частоту можно настроить по рассвету/закату.`:'Задайте границы, частоту и длительность запуска в конструкторе.');};

async function loadConfig(){try{const d=await api('/api/config');$('#cfg-ssid').value=d.ssid||'';$('#cfg-tz').value=Number.isFinite(+d.tz)?d.tz:3;$('#cfg-lat').value=Number.isFinite(+d.lat)?d.lat:'';$('#cfg-lon').value=Number.isFinite(+d.lon)?d.lon:'';$('#cfg-pass').value='';$('#cfg-pass').placeholder=d.has_pass?'Пароль сохранён — оставьте пустым':'Введите пароль сети'}catch(e){toast('Настройки не загружены',e.message,'error',5000)}}
$('#pass-toggle').onclick=()=>{const i=$('#cfg-pass'),show=i.type==='password';i.type=show?'text':'password';$('#pass-toggle').textContent=show?'×':'◉'};$('#tz-browser').onclick=()=>{$('#cfg-tz').value=-new Date().getTimezoneOffset()/60;toast('Часовой пояс подставлен',`UTC${+$('#cfg-tz').value>=0?'+':''}${$('#cfg-tz').value}`)};$('#btn-save-config').onclick=async()=>{const ssid=$('#cfg-ssid').value.trim(),pass=$('#cfg-pass').value,tz=+$('#cfg-tz').value,lat=+$('#cfg-lat').value,lon=+$('#cfg-lon').value;if(!ssid||ssid.length>32){toast('Проверьте SSID','Название сети должно содержать 1–32 символа.','warn');return}if(pass.length>63||tz<-12||tz>14||lat<-90||lat>90||lon<-180||lon>180){toast('Проверьте настройки','Пароль до 63 символов, UTC от −12 до +14, координаты в допустимых пределах.','warn');return}if(!await ask('Применить настройки сети?','Контроллер сохранит параметры и перезагрузится.','Сохранить'))return;try{await api('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({ssid,pass,tz,lat,lon})});toast('Настройки сохранены','Контроллер перезагружается.', 'ok',6000);setConnection(false)}catch(e){toast('Не удалось сохранить',e.message,'error',5000)}};
async function loadDiagnostics(){try{const d=await api('/api/diagnostics');$('#diag-version').textContent=`${d.version||'dev'} · ${d.build||'?'}`;$('#diag-heap').textContent=bytes(d.free_heap);$('#diag-minheap').textContent=bytes(d.min_free_heap);$('#diag-flash').textContent=bytes(d.flash_size);$('#diag-sketch').textContent=bytes(d.sketch_size);$('#diag-reset').textContent=d.reset_reason_text||'—';$('#diag-api').textContent='v'+(d.api_version??'—')}catch(e){toast('Диагностика недоступна',e.message,'error',5000)}}$('#btn-refresh-diag').onclick=loadDiagnostics;
function eventView(e){let title='Системное событие',detail='',klass='';if(e.type==='boot'){title='Контроллер запущен';detail=e.value?'Автоматика была активна':'Автоматика была на паузе'}else if(e.type==='pump_start'){title='Насос запущен';detail=`${sourceLabel[e.source]||e.source} · ${e.value} с`;klass='pump'}else if(e.type==='pump_stop'){title='Насос остановлен';detail=`${sourceLabel[e.source]||e.source}${reasonLabel[e.reason]?' · '+reasonLabel[e.reason]:''}`;klass=e.reason==='timeout'?'pump':'warn'}else if(e.type==='automation_enabled'){title='Автоматика возобновлена';detail='Плановые циклы разрешены';klass='pump'}else if(e.type==='automation_paused'){title='Автоматика на паузе';detail='Плановые циклы запрещены';klass='warn'}else if(e.type==='schedule_changed'){title='Расписание изменено';detail=`${e.value} циклов`}else if(e.type==='hydraulics_saved'){title='Гидравлика сохранена';detail=e.value?`${(e.value/1000).toFixed(3)} л/мин`:'Калибровка очищена'}else if(e.type==='config_changed'){title='Изменены сетевые настройки'}else if(e.type==='ota_started'){title='Запущено OTA-обновление';klass='warn'}else if(e.type==='reboot_requested'){title='Запрошена перезагрузка';klass='warn'}const time=e.timestamp?e.timestamp.slice(11):`+${fmtUptime(e.uptime)}`;return{title,detail,klass,time}}
function renderEvents(target,events,limit=32){const box=$(target);box.innerHTML='';const list=events.slice(0,limit);if(!list.length){box.innerHTML='<div class="empty">В этой сессии событий пока нет.</div>';return}for(const e of list){const v=eventView(e),row=document.createElement('div');row.className='event '+v.klass;row.innerHTML='<div class="event-time"></div><span class="event-dot"></span><div><div class="event-title"></div><div class="event-detail"></div></div>';row.querySelector('.event-time').textContent=v.time;row.querySelector('.event-title').textContent=v.title;row.querySelector('.event-detail').textContent=v.detail;box.appendChild(row)}}
async function loadEvents(){try{const d=await api('/api/events'),events=d.events||[];renderEvents('#event-preview',events,4);renderEvents('#event-list',events,32)}catch(e){if(activeTab==='system')toast('Журнал недоступен',e.message,'error',5000)}}$('#btn-refresh-events').onclick=loadEvents;$('#btn-clear-events').onclick=async()=>{if(!await ask('Очистить журнал сессии?','Это не влияет на настройки и расписание.','Очистить'))return;try{await api('/api/events/clear',{method:'POST'});await loadEvents();toast('Журнал очищен')}catch(e){toast('Не удалось очистить',e.message,'error',5000)}};

function downloadJson(name,obj){const blob=new Blob([JSON.stringify(obj,null,2)+'\n'],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
$('#btn-export').onclick=async()=>{try{const [status,schedule,config,hydraulics]=await Promise.all([api('/api/status'),api('/api/schedule'),api('/api/config'),api('/api/hydraulics')]);downloadJson('hydroesp-backup.json',{format:'hydroesp-backup-v2',created:new Date().toISOString(),device:{version:status.version,build:status.build,ssid:config.ssid,tz:config.tz},automation_enabled:!!status.automation_enabled,hydraulics,schedule});toast('Резервная копия готова','Пароль Wi‑Fi не экспортирован.')}catch(e){toast('Экспорт не выполнен',e.message,'error',5000)}};$('#btn-import').onclick=()=>$('#import-file').click();$('#import-file').onchange=async()=>{const f=$('#import-file').files[0];if(!f)return;try{const data=JSON.parse(await f.text()),slots=Array.isArray(data)?data:data.schedule;if(!Array.isArray(slots))throw new Error('В файле нет массива schedule');const box=$('#schedule-list');box.innerHTML='';slots.slice(0,MAX_SLOTS).forEach(s=>box.appendChild(slotRow(s.h,s.m,s.d)));scheduleLoaded=true;updateScheduleSummary();markScheduleDirty();switchTab('schedule');toast('Импортирован черновик','Контроллер ещё не изменён. Проверьте график и нажмите «Сохранить».')}catch(e){toast('Импорт не выполнен',e.message,'error',5000)}finally{$('#import-file').value=''}};
$('#btn-reboot').onclick=async()=>{if(!await ask('Перезагрузить контроллер?','Активный насос будет остановлен. Настройки и расписание сохранятся.','Перезагрузить',true))return;try{await api('/api/reboot',{method:'POST'});toast('Перезагрузка','Соединение временно прервётся.', 'ok',6000);setConnection(false)}catch(e){toast('Команда не отправлена',e.message,'error',5000)}};

const dz=$('#drop-zone'),fi=$('#ota-file');dz.onclick=()=>fi.click();dz.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();fi.click()}};dz.ondragover=e=>{e.preventDefault();dz.classList.add('drag')};dz.ondragleave=()=>dz.classList.remove('drag');dz.ondrop=e=>{e.preventDefault();dz.classList.remove('drag');if(e.dataTransfer.files[0])prepareFirmware(e.dataTransfer.files[0])};fi.onchange=()=>{if(fi.files[0])prepareFirmware(fi.files[0])};async function prepareFirmware(file){if(!file.name.toLowerCase().endsWith('.bin')){toast('Неверный файл','Нужен .bin образ прошивки.','warn');return}if(file.size<10000){toast('Файл слишком маленький','Проверьте выбранную сборку.','warn');return}if(!await ask('Обновить прошивку?',`${file.name}, ${Math.round(file.size/1024)} КБ. Насос будет остановлен перед записью.`,'Обновить',true))return;uploadFirmware(file)}
function uploadFirmware(file){const fd=new FormData();fd.append('file',file);const xhr=new XMLHttpRequest();xhr.open('POST','/ota/upload',true);$('#ota-progress').classList.remove('hidden');$('#ota-text').textContent=file.name;$('#ota-bar').style.width='0%';$('#ota-percent').textContent='0%';xhr.upload.onprogress=e=>{if(e.lengthComputable){const p=Math.round(e.loaded/e.total*100);$('#ota-bar').style.width=p+'%';$('#ota-percent').textContent=p+'%'}};xhr.onload=()=>{if(xhr.status===200&&xhr.responseText.trim()==='OK'){toast('Прошивка записана','Контроллер перезагружается.','ok',6000);$('#ota-text').textContent='Готово. Перезагрузка…';setConnection(false)}else{toast('Ошибка OTA',xhr.responseText||`HTTP ${xhr.status}`,'error',6000);$('#ota-text').textContent='Ошибка обновления'}};xhr.onerror=()=>{toast('Соединение прервано','Не удалось завершить OTA.','error',6000);$('#ota-text').textContent='Ошибка соединения'};xhr.send(fd)}

try{const saved=JSON.parse(localStorage.getItem('hydro_cal_samples')||'[]');if(Array.isArray(saved))calSamples=saved.filter(x=>Number.isFinite(+x.flow)&&Number.isFinite(+x.ml)&&Number.isFinite(+x.duration)).slice(0,MAX_CAL_SAMPLES)}catch(_){calSamples=[]}calDuration=+localStorage.getItem('hydro_cal_duration')||30;calTestDuration=+localStorage.getItem('hydro_last_cal_test_duration')||calDuration;$$('.cal-duration').forEach(b=>b.classList.toggle('active',+b.dataset.calDuration===calDuration));renderCalSamples();initScheduleBuilder();
const requestedTab=new URLSearchParams(location.search).get('tab');if(requestedTab&&tabMeta[requestedTab])switchTab(requestedTab);
updateStatus();loadEvents();setInterval(()=>{updateStatus();if(activeTab==='system')loadEvents()},2000);
</script>
</body>
</html>
)rawliteral";
